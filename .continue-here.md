# ⏸ CONTINUE HERE — точка возобновления работы

> Файл передачи состояния между сессиями (GSD-стиль; проект ведётся вручную, без `.planning/`).
> Обновлён: 2026-06-20 (+05). Проект: **taxpfg.kz** (сайт ТОО «PRIMEFINANCE GROUP»).
> Возобновление: прочитай этот файл → `docs/DEVELOPMENT_PLAN.md` (все этапы) + `docs/HANDOFF.md` (что от клиента).

---

## 1. Где мы сейчас

**Все 7 этапов (0–7) ЗАВЕРШЕНЫ и закоммичены. Рабочее дерево чистое.**
Сайт технически **production-ready**: 10 страниц, адаптив (1440/768/390), чистая консоль,
рабочие ссылки/картинки, единые контакты, уникальные title/description, OpenGraph/JSON-LD,
`sitemap.xml` + `robots.txt`.

Последние коммиты:
- `da719a4` — Этап 7 (Хендофф): `docs/HANDOFF.md` — чек-лист зависимостей клиента.
- `deb73b1` — Этап 6 (QA): сквозная проверка + фикс 2 дефектов.
- `2cbaf6f` — Этап 4 (Изображения); `bb1e013` — Этап 5 (SEO).

В этой сессии закрыты **Этап 6** (QA: найдено и исправлено 2 дефекта — пустые секции FAQ
на 5 сервис-страницах скрыты резервным комментарием; битый `@import` Google Fonts в
`css/base.css` исправлен) и **Этап 7** (создан `docs/HANDOFF.md`). Детали — `docs/SESSION_LOG.md`.

**К публикации сайт НЕ готов** — но только из-за недостающих данных клиента (см. трек B ниже).

---

## 2. Что делать дальше — два независимых трека

### Трек A — опциональная техполировка (НЕ требует клиента) ← был начат и прерван
Когда прервали, я только что поднял сервер и собирался запустить Lighthouse-аудит.
Все пункты безопасны и не требуют выдумывания фактов:

1. **Lighthouse-аудит** (accessibility / SEO / best-practices, режим navigation, desktop) на
   `index.html` + одной внутренней — получить объективные находки, чинить по приоритету.
2. **BreadcrumbList JSON-LD** на 8 внутренних страницах (там есть видимые хлебные крошки
   «Главная / …») — фактическая разметка, через `seo_common.py`/`assemble.py`.
3. **Пер-файловый `lastmod`** в `sitemap.xml` (сейчас общий).
4. **Проверки:** `alt` у всех `<img>`, наличие кастомной **404-страницы** (сейчас нет),
   `loading="lazy"` для нефолд-картинок.
5. _(опц., отложено)_ **h1:** тема даёт логотипу `<h1 class="site-title">` → у внутренних
   2×h1. HTML5-валидно; правка трогает общую шапку и требует отдельного h1 для index —
   см. `DEVELOPMENT_PLAN.md` §11.

**Первое конкретное действие трека A:** поднять
`PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m http.server 8127 --bind 127.0.0.1`,
прогнать Lighthouse (chrome-devtools MCP), затем внедрять безопасные правки и пересобирать.

### Трек B — публикация (БЛОКИРОВАНА данными клиента)
Раздел **A** в `docs/HANDOFF.md` (🔴): точное юр. наименование, **БИН**, ответственный за
обработку ПДн (ст. 25 № 94-V), срок хранения, дата вступления Политики, **рабочий e-mail**,
**график работы**. Без них Политику конфиденциальности публиковать нельзя (юр. требование
Закона № 94-V и поправок № 231-VIII). Выдумывать запрещено.

---

## 3. Данные компании (единый источник)

- Бренд (отображение): **PrimeFinance Group**; юр.: ТОО «PRIMEFINANCE GROUP» (подтвердить).
- Телефон: **+7 707 237 00 50** → `tel:+77072370050`, `https://wa.me/77072370050`.
- Адрес: г. Алматы, пр. Абая, 68/74, БЦ «AVENUE CITY», офис 39.
- Директор: Сарсенбаева С. А. (в черновике; решить, публиковать ли).
- E-mail: `info@taxpfg.kz` — **предварительный** (домен сайта), подтвердить у клиента.
- БИН `260640025396` — **под вопросом**, подтвердить.

---

## 4. Инфраструктура и важные заметки (анти-паттерны сессии)

- **Фоновый сервер остановлен** (порт 8127 свободен). Для QA поднять заново (см. трек A).
- **Сборка детерминирована и идемпотентна:** `.gsd-build/{assemble.py, build_privacy.py,
  patch_index_ogimage.py, hide_empty_faq.py}`. Внутренние страницы = правка фрагмента
  `.gsd-build/frag/<page>.content.html` → `assemble.py`. `index.html` и **футер**
  (`.gsd-build/template.html`) правятся напрямую. Общие SEO/og — `seo_common.py`.
- **FAQ:** в нужном фрагменте найти `<!-- [PFG] FAQ-резерв … -->`, вернуть секцию
  `<section class="pfg-section …">` с аккордеоном, заполнить Q&A, пересобрать.
- **`css/base.css` — вендорный.** Фикс шрифтов внесён туда **вынужденно** (из `custom.css`
  `@import` неустраним). Любое ОФОРМЛЕНИЕ — только через `css/custom.css` / `js/custom.js`,
  не через вендорные файлы темы.
- **Шапка/подвал байт-идентичны** на всех страницах (nav `8513764f`, footer `548c42b1`) —
  при правке `template.html` держать синхронными и перепроверять md5.
- **Windows-консоль cp1251:** префикс `PYTHONIOENCODING=utf-8 PYTHONUTF8=1` для python;
  пути — forward slashes; `grep -c` при 0 совпадений выходит с кодом 1 (ломает `&&`).
- **Никаких выдумок:** FAQ, команда, кейсы, цифры, цены, реквизиты — резерв до клиента.

---

## 5. Как возобновить

```
1. Прочитать этот файл (.continue-here.md).
2. Открыть docs/DEVELOPMENT_PLAN.md (все этапы [x]) и docs/HANDOFF.md (раздел A — блокеры публикации).
3. cd C:/Users/Администратор/Desktop/taxpfg.kz
4. Если есть данные клиента → внести по docs/HANDOFF.md и пересобрать (трек B).
   Если нет → продолжить опциональную техполировку (трек A): сервер + Lighthouse.
5. Атомарные коммиты; после правок — быстрый QA (консоль/ссылки/адаптив).
```

**Следующее конкретное действие:** трек A — поднять сервер на 8127 и запустить Lighthouse-аудит
(`index.html`, desktop, navigation), затем чинить находки по приоритету.
