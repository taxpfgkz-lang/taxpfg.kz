---
phase: 04-conversion-blocks-imagery
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - baseline/measure-phase4.cjs
autonomous: false
requirements: [CNV-01, CNV-02, CNV-03, CNV-04, IMG-01]
must_haves:
  truths:
    - "All 5 requirements (CNV-01..04, IMG-01) are proven by DOM measurement at 1440/1024/768/390/360, not by reading CSS source"
    - "axe = 0 new violations and Lighthouse a11y >= 95 floor on the touched pages (index, services) — AUD-01 floor not regressed"
    - "VER-04 behavior smoke passes: form->WhatsApp, menu, marquee, slider, reduced-motion all still work; js/custom.js byte-identical"
    - "Pricing CTAs route to wa.me/77072370050; footer parity holds on 11; no horizontal scroll at any of the 5 viewports"
  artifacts:
    - "baseline/measure-phase4.cjs: ephemeral Playwright script capturing pricing/hero/footer/image/title-bar metrics at 5 viewports"
    - "04-04-SUMMARY.md: pass/fail table per requirement vs the AUD-01 floor + the honest final !important count"
  key_links:
    - "Serve via python -m http.server 8080 --bind 127.0.0.1 — never file:// (breaks fonts/relative paths/measurement, AUD-02/Pitfall 5)"
    - "Trust the rendered getBoundingClientRect box, never the CSS source text (FT1/Pitfall 3)"
---

<objective>
Phase gate for Phase 4: prove all five requirements (CNV-01..04, IMG-01) by DOM measurement at the 5 viewports, confirm the AUD-01 a11y floor (axe=0, Lighthouse a11y>=95) is not regressed, run the VER-04 behavior smoke (js byte-identical), and record the honest final `!important` count. This is a human-verify gate (autonomous:false) because conversion visuals need an eyeball pass.

Purpose: this site has no unit tests — DOM-measured Playwright + axe + Lighthouse + behavior smoke is the only QA, and the Workflow proxy may fail (memory note) so critical checks are duplicated manually.
Output: an ephemeral measure script + a pass/fail SUMMARY table + a human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-conversion-blocks-imagery/04-RESEARCH.md
@.planning/phases/04-conversion-blocks-imagery/04-UI-SPEC.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md

# Touched pages / sources:
@index.html
@services.html
@css/custom.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — build the Phase-4 DOM measure script</name>
  <files>baseline/measure-phase4.cjs</files>
  <read_first>
    - 04-RESEARCH.md §"Validation Architecture" → "Phase Requirements -> Test Map" and "Wave 0 Gaps" — the exact metrics each requirement needs and the reuse-the-baseline/measure*.cjs pattern.
    - .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md — the AUD-01 floor (a11y>=95, axe=0) and the HE/T1/I/FT findings being closed.
    - existing `baseline/measure*.cjs` (if present) for the established Playwright runner shape.
  </read_first>
  <action>
    Create `baseline/measure-phase4.cjs` — an ephemeral Playwright script (no persistent test-framework convention exists) that serves nothing itself but assumes `python -m http.server 8080 --bind 127.0.0.1` is running from repo root (NEVER file://). For each viewport in [1440, 1024, 768, 390, 360] it loads `http://127.0.0.1:8080/services.html` and `/index.html` and captures via `getBoundingClientRect`/DOM:
    - CNV-02 (services): count `.pfg-pricing-tier` == 3; `.pfg-pricing-tier--popular` == 1; the 3 tiers' `.height` equal within tolerance when on one row (>=768); every `.pfg-pricing-tier a.pbmit-btn` `href` contains `wa.me/77072370050`.
    - CNV-01 (index): each `.swiper-slide` has exactly one `.pbmit-btn`; `.pbmit-slider-title` computed `line-height` >= `font-size` at 1440/1024.
    - CNV-04: footer `border-top` == `3px solid rgb(236, 171, 35)`; footer rendered height @390 is sane (record value, compare to AUD-01 FT3 1732px — should not increase).
    - IMG-01: for each photo `<img>`, naturalWidth/naturalHeight ratio vs rendered width/height ratio within tolerance (no distortion).
    - T1: `.pbmit-title-bar-wrapper` rendered height @768 < 550 and > the mobile floor; @390 collapse unchanged.
    - global: `document.documentElement.scrollWidth <= clientWidth` (no horizontal scroll) at every viewport on both pages.
    Print a per-check PASS/FAIL line and exit non-zero on any FAIL. This is throwaway tooling (matches the `baseline/` ad-hoc convention) — not a committed test suite.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -c baseline/measure-phase4.cjs && echo "measure-phase4.cjs parses OK"</automated>
  </verify>
  <done>
    `baseline/measure-phase4.cjs` exists, parses, and encodes the CNV-01/02/04, IMG-01, T1, and no-h-scroll checks across the 5 viewports against http://127.0.0.1:8080.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the full phase gate (DOM + axe + Lighthouse + VER-04 smoke)</name>
  <files>baseline/measure-phase4.cjs</files>
  <read_first>
    - 04-RESEARCH.md §"Validation Architecture" → "Sampling Rate" (phase gate = full suite green @5vp) + §"Common Pitfalls" (serve over http, trust DOM not CSS, `!important` comment-prose creep).
    - 04-UI-SPEC.md §"Checker Sign-Off" — the 6 dimensions to confirm.
  </read_first>
  <action>
    Start `python -m http.server 8080 --bind 127.0.0.1` from repo root. Run `node baseline/measure-phase4.cjs` and confirm every check PASSes at all 5 viewports.
    Run axe (`@axe-core/playwright` 4.12.1) on index.html and services.html — confirm 0 new violations vs the AUD-01 baseline (WCAG 2.0/2.1 A+AA), with `color-contrast` == 0 (the new pricing badge text-on-gold must hold AA).
    Run Lighthouse (13.4.0) accessibility on both pages — confirm score >= 95 floor.
    VER-04 behavior smoke (reuse the Phase-3 smoke): submit `.pfg-form` on contacts.html and confirm it builds a `wa.me/77072370050?text=...` link; toggle the mobile menu; confirm the marquee is running; confirm the hero slider advances; confirm `prefers-reduced-motion` still only kills the scoped decorative motion. Confirm `git diff HEAD js/custom.js` is empty (byte-identical).
    Record the honest final filtered `!important` count (`grep -v '^[[:space:]]*/\*' css/custom.css | grep -v '^[[:space:]]*\*' | grep -c '!important'`): expect 59, or 60 if the T1 tier added one — if 60, the SUMMARY must justify it citing shortcode.css:4934.
    Clean up: stop the http.server. The measure script is ephemeral.
    If any check FAILs, do NOT mark the phase complete — record the failure in the SUMMARY for gap-closure replanning.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && git diff HEAD --quiet -- js/custom.js && echo "VER-04 js byte-identical OK"; echo "final important count:"; grep -v '^[[:space:]]*/\*' css/custom.css | grep -v '^[[:space:]]*\*' | grep -c '!important'</automated>
  </verify>
  <done>
    measure-phase4.cjs is all-green at 5 viewports; axe = 0 new violations (color-contrast 0); Lighthouse a11y >= 95 on index + services; VER-04 smoke passes and js/custom.js is byte-identical; final filtered `!important` count is 59 or a justified 60; results captured in 04-04-SUMMARY.md as a per-requirement pass/fail table.
  </done>
</task>

<task type="auto">
  <name>Task 3: Human-verify the conversion + imagery layer (visual sign-off)</name>
  <files></files>
  <action>
    Final human visual confirmation of Phase 4 after all automated gates pass (measure-phase4.cjs green @5vp, axe=0, Lighthouse a11y>=95, VER-04 smoke, `!important`<=60), recorded in 04-04-SUMMARY.md. This is not code — it is the eyeball pass on the conversion visuals (pricing, hero, footer, title-bar, imagery).
  </action>
  <verify>
    <human-check>
      1. Serve locally: `python -m http.server 8080 --bind 127.0.0.1` from repo root.
      2. Open http://127.0.0.1:8080/services.html — scroll to the pricing block (above the FAQ). Confirm: exactly 3 tiers, exactly one gold-highlighted «популярный» tier, each tier has a «Получить консультацию» button that opens WhatsApp (wa.me/77072370050) in a new tab. Resize to mobile (390) — tiers stack cleanly, no horizontal scroll.
      3. Open http://127.0.0.1:8080/index.html — the hero headline is not vertically clipped at desktop width, each slide shows one button, the trust/value line reads above the fold.
      4. Check the footer on index, contacts, privacy at 390 — unified look, gold top-border present, not excessively tall.
      5. Open about.html at tablet width (768) — the title band is shorter/proportionate, not a 550px block.
      6. No image looks stretched/squashed.
    </human-check>
  </verify>
  <done>Human confirmed: pricing shows 3 tiers + one gold highlight + per-tier WhatsApp CTA, hero headline not clipped with one CTA/slide, footer unified across pages, title-bar proportionate on tablet, imagery undistorted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local http.server | Loopback-only (`--bind 127.0.0.1`) audit server; not network-exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Spoofing | local audit server | low | mitigate | Bind to 127.0.0.1 only; ephemeral; stopped after the gate run |
| T-04-07 | Repudiation | a11y floor regression slipping through | medium | mitigate | DOM + axe + Lighthouse measured against the AUD-01 baseline; human-verify gate before phase completion |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase; Playwright/axe/Lighthouse already present from Phases 1/3 |
</threat_model>

<verification>
- measure-phase4.cjs green at 1440/1024/768/390/360 on index + services.
- axe = 0 new violations (color-contrast 0); Lighthouse a11y >= 95.
- VER-04 smoke green; `git diff HEAD js/custom.js` empty.
- Final filtered `!important` count 59 or justified 60.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
- All 5 requirements proven by DOM measurement at 5 viewports; AUD-01 floor not regressed; VER-04 behavior-identical; human sign-off obtained.
</success_criteria>

<output>
Create `.planning/phases/04-conversion-blocks-imagery/04-04-SUMMARY.md` when done
</output>
