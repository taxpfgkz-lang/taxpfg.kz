---
phase: 04-conversion-blocks-imagery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services.html
  - css/custom.css
autonomous: true
requirements: [CNV-02]
must_haves:
  truths:
    - "services.html renders a pricing block of exactly 3 tiers between the «Получить консультацию» CTA section and the FAQ block"
    - "Exactly one tier carries the gold «популярный» highlight; the other two carry no gold fill"
    - "Each tier has its own CTA linking to the existing wa.me/77072370050 direct-link flow"
    - "Same-row tiers are visually equal-height with no horizontal overflow at any of the 5 viewports"
  artifacts:
    - "services.html: net-new <section class=\"pfg-section pfg-pricing-section\"> with .pfg-grid + 3 .pfg-card tiers (per D CNV-02)"
    - "css/custom.css: .pfg-pricing-* styling rules composed from --pfg-* tokens"
    - "css/custom.css: pricing-card hover entry added to the scoped prefers-reduced-motion block"
  key_links:
    - "Pricing CTA href → https://wa.me/77072370050 (existing direct-link arm; NOT the form arm, NOT new JS)"
    - "Tier card inherits .pfg-card (height:100%, hairline, shadow, hover-lift) + .pfg-grid (align-items:stretch) for equal-height rows"
---

<objective>
Build the net-new pricing block on services.html (CNV-02): three service-package tiers with exactly one gold «популярный» highlight and a per-tier WhatsApp CTA, composed entirely from Phase-3 primitives (`.pfg-card`, `.pbmit-btn`, `.pfg-grid`) and Phase-2 tokens.

Purpose: services.html currently has zero pricing markup. This block gives prospects a clear package comparison and a single dominant action per tier, converting browse → lead via the existing WhatsApp flow.
Output: net-new `services.html` pricing section + `.pfg-pricing-*` CSS rules in `css/custom.css`. No JS edit (VER-04). Page-specific — NOT shared chrome, NOT change-all-11.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-conversion-blocks-imagery/04-CONTEXT.md
@.planning/phases/04-conversion-blocks-imagery/04-RESEARCH.md
@.planning/phases/04-conversion-blocks-imagery/04-UI-SPEC.md

# Live source the executor must read before editing:
@services.html
@css/custom.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert net-new pricing markup into services.html</name>
  <files>services.html</files>
  <read_first>
    - services.html:395-470 — the existing «Дополнительно» `.pfg-grid`/`.pfg-card` pattern (:401-418), the «Получить консультацию» CTA `.pbmit-btn` markup (:431-448, primary `.pbmit-btn` + secondary `.pbmit-btn white` with `target="_blank" rel="noopener"`), the CTA section close at :450, the FAQ comment at :452.
    - 04-RESEARCH.md §"Pattern 1" and §"Pattern 2" — exact tier composition and the direct-link WhatsApp arm (NOT the form arm).
    - 04-UI-SPEC.md §CNV-02 + §Copywriting Contract — copy rules (Russian sentence case, no exact figures).
  </read_first>
  <action>
    Insert a new `<section class="pfg-section pfg-pricing-section">` into services.html in the gap between the closing `</section>` of the «Получить консультацию» CTA block (currently line 450) and the FAQ comment `<!-- [PFG] FAQ-аккордеон ... -->` (currently line 452). Do not modify the CTA section or the FAQ section — insert only.
    Structure: a `.pbmit-heading-subheading` header (`<p class="pbmit-subtitle">Тарифы</p>` + `<h2 class="pbmit-title">` with a Russian sentence-case headline about accounting-support packages), then `<div class="pfg-grid pfg-pricing">` containing exactly 3 `<div class="pfg-card pfg-pricing-tier">` tiers.
    Tier content (names/scope at your discretion, Russian sentence case, meaningful for accounting/tax — e.g. ИП на упрощёнке / ТОО на общем режиме / Премиум): an `<h3>` tier name, a `<p class="pfg-pricing-price">` price line using «от … ₸ / мес» or «по запросу» (NO exact figures — D CNV-02 deferred to v2), a `<ul>` of scope-of-service bullets, and a per-tier CTA.
    Mark EXACTLY ONE tier (the middle/recommended one) with the additional modifier class `pfg-pricing-tier--popular` and give it a `<span class="pfg-pricing-badge">Популярный</span>` as its first child.
    CTA wiring (CNV-02 / VER-04): each tier CTA is an `<a href="https://wa.me/77072370050" target="_blank" rel="noopener">` using the existing `.pbmit-btn` nested-span markup (copy the exact span structure from services.html:431-438). The popular tier CTA is the gold-fill primary (`class="pbmit-btn"`); the two sibling tiers step down to `class="pbmit-btn white"` so exactly one prominent primary remains. This is the direct-link arm — do NOT use a form, do NOT add or call any JS. CTA label «Получить консультацию» (or close variant).
    Security: every CTA MUST carry `target="_blank" rel="noopener"` (reverse-tabnabbing mitigation, matches site convention).
    Do NOT touch any other page — pricing is page-specific.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(grep -c 'pfg-pricing-tier"' services.html)" = "2" -o "$(grep -c 'pfg-pricing-tier' services.html)" -ge "3"; grep -c 'pfg-pricing-tier--popular' services.html; grep -c 'wa.me/77072370050' services.html</automated>
  </verify>
  <done>
    services.html contains a `.pfg-pricing-section` with exactly 3 `.pfg-pricing-tier` cards (grep count of `pfg-pricing-tier` ≥ 3, including the popular one), exactly one `pfg-pricing-tier--popular` (grep = 1), each tier CTA points at `https://wa.me/77072370050` with `target="_blank" rel="noopener"`, no exact price figures, no JS added, no other HTML file changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Style pricing tiers in css/custom.css</name>
  <files>css/custom.css</files>
  <read_first>
    - css/custom.css:289-346 — `--pfg-*` tokens (space/fs/lh/gold/ink/radius/shadow) to compose from. No hardcoded hex/px.
    - css/custom.css:544-557 — `.pfg-card` hairline + shadow + `:hover{transform:translateY(-4px)}` lift the tier inherits.
    - css/custom.css:377-391 — the scoped `prefers-reduced-motion` block where decorative hover entries belong.
    - css/custom.css:1147 area — `.pfg-grid{align-items:stretch; grid-template-columns:repeat(auto-fit,minmax(260px,1fr))}` (equal-height rows for free).
    - 04-RESEARCH.md §"Code Examples → Pricing styling" — the token-composed rule shape.
  </read_first>
  <action>
    Append a new banner-commented stage block (follow the «ЭТАП N — …» Russian banner-comment convention, e.g. «ЭТАП 15 — CNV-02 pricing (services.html only, не shared chrome)») to css/custom.css.
    Add `.pfg-pricing-tier` as a flex column (`display:flex; flex-direction:column; gap:var(--pfg-space-4)`) so the CTA can be pinned to the bottom for an even row — set `.pfg-pricing-tier .pbmit-btn{ margin-top:auto; }`.
    Style `.pfg-pricing-price` with `font-size:var(--pfg-fs-h4)`, `font-weight:600`, `color:var(--pfg-ink)`.
    Style `.pfg-pricing-tier--popular` as the single gold highlight: `border-color:var(--pfg-gold)` + `box-shadow:var(--pfg-shadow-md)`. Do NOT gold-fill the whole card — the highlight is the gold border/badge, keeping AA-readable body text (master accent reserved-for: single popular pricing tier).
    Style `.pfg-pricing-badge`: `align-self:flex-start; background:var(--pfg-gold); color:#1b1b1b` (text-on-gold holds AA), `font-size:var(--pfg-fs-label)`, padding via `--pfg-space-*`, `border-radius:var(--pfg-radius-sm)`, and `text-transform:none` (Russian sentence case — the theme would otherwise capitalize).
    The tier already inherits `.pfg-card` hover-lift; if you add any pricing-specific decorative transition, add its selector to the existing scoped `prefers-reduced-motion` block (377-391) — never a universal killer.
    Use tokens only — NO hardcoded hex (except the established `#1b1b1b` text-on-gold already used elsewhere) and NO hardcoded px. Add NO net-new `!important` in this block.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'pfg-pricing-tier' css/custom.css; grep -c 'pfg-pricing-tier--popular' css/custom.css; grep -c 'pfg-pricing-badge' css/custom.css; test "$(grep -v '^[[:space:]]*/\*' css/custom.css | grep -v '^[[:space:]]*\*' | grep -c '!important')" -le "59"</automated>
  </verify>
  <done>
    css/custom.css defines `.pfg-pricing-tier`, `.pfg-pricing-tier--popular` (gold border highlight, no full gold fill), `.pfg-pricing-price`, and `.pfg-pricing-badge` (text-transform:none), all composed from `--pfg-*` tokens; any decorative pricing motion is inside the scoped reduced-motion block; filtered `!important` count stays ≤ 59 (no net-new in this plan).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page → WhatsApp (wa.me) | User leaves the site via a new tab to an external host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | Pricing CTA `target="_blank"` anchors | medium | mitigate | Every pricing CTA carries `rel="noopener"` (reverse-tabnabbing), matching site convention (`services.html:440`) |
| T-04-02 | Tampering | Pricing copy | low | accept | All pricing copy is static, author-controlled Russian text; no dynamic interpolation |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed/referenced this phase; no `package.json`, no registry — nothing to audit |
</threat_model>

<verification>
- `git diff HEAD js/custom.js` is empty (VER-04 — pricing uses direct links, no JS).
- Only services.html among the 11 HTML files changed (page-specific; NOT change-all-11).
- Filtered `!important` count in css/custom.css stays ≤ 59.
- Deeper DOM checks (3-tier count, single highlight, equal-row-height, no h-scroll, CTA→wa.me) run in the Wave-4 verification plan via Playwright at 5 viewports.
</verification>

<success_criteria>
- services.html shows a 3-tier pricing block with exactly one gold «популярный» highlight, each tier CTA → wa.me/77072370050 direct link (`target="_blank" rel="noopener"`), no exact figures.
- `.pfg-pricing-*` rules are token-composed, no net-new `!important`.
- js/custom.js byte-identical; no other HTML page touched.
</success_criteria>

<output>
Create `.planning/phases/04-conversion-blocks-imagery/04-01-SUMMARY.md` when done
</output>
