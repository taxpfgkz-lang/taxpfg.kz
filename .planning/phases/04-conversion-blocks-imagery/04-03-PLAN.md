---
phase: 04-conversion-blocks-imagery
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - css/custom.css
autonomous: true
requirements: [CNV-04, IMG-01]
must_haves:
  truths:
    - "Footer renders with unified appearance across all 11 pages (gold 3px top-border preserved everywhere)"
    - "Footer vertical density on mobile (390) is tightened via tokens without regressing the FT1/F2 44px tap-zones"
    - "Photo <img> elements use object-fit so proportions are preserved with no distortion at any viewport"
    - "Title-bar min-height shrinks below 550px at 768 (tablet tier) without regressing the >=390 collapse"
  artifacts:
    - "css/custom.css: footer mobile-density polish (token-driven, CSS-only — no footer markup edit)"
    - "css/custom.css: IMG-01 object-fit rules scoped to theme photo wrappers (not logos/icons)"
    - "css/custom.css: T1 title-bar @media(max-width:768px) min-height tier (the one likely net-new !important, vendor-cited)"
  key_links:
    - "Footer parity is CSS-only — per-page .active nav state + 2 content strings are legitimate and must NOT be flattened (no change-all-11 markup edit)"
    - "T1 tier beats shortcode.css:4934 min-height:550px by source-order; the !important (if required) cites the vendor rule in a Russian comment without the literal important-token"
    - "object-fit targets theme photo wrappers, not bare img (would distort logos/icons) — verify selector against live DOM (A5)"
---

<objective>
Unify the footer appearance across all 11 pages (CNV-04), normalize imagery sizing/proportions (IMG-01), and add the tablet title-bar min-height tier (T1) — all CSS-only in css/custom.css. Footer parity is achieved through CSS, NOT by flattening the legitimately-divergent footer markup (per-page `.active` nav + 2 content strings stay). T1 is the single most likely net-new `!important` this phase and must cite the beaten vendor rule.

Purpose: a credible, identical-looking footer reinforces trust on every page; distorted/oversized imagery and a disproportionate 550px tablet title band read as "cheap". These are the remaining visual-consistency defects (FT3 / I1 / T1) from the Phase-1 audit.
Output: footer-density, object-fit, and title-bar-tier rules in css/custom.css. No HTML edit (footer parity is CSS-only here). No JS edit.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-conversion-blocks-imagery/04-CONTEXT.md
@.planning/phases/04-conversion-blocks-imagery/04-RESEARCH.md
@.planning/phases/04-conversion-blocks-imagery/04-UI-SPEC.md

# Live source the executor must read before editing:
@css/custom.css
@css/shortcode.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer parity + mobile density (CNV-04, CSS-only)</name>
  <files>css/custom.css</files>
  <read_first>
    - css/custom.css:583 (`.site-footer{ border-top:3px solid var(--pfg-gold) }`), :862 (footer overflow-x), :216-224 (footer menu li / text-transform), :1240-1246 (footer menu tap-zone >=44px — FT1/F2, do NOT regress).
    - 04-UI-SPEC.md §CNV-04 — parity is CSS; gold 3px top-border preserved; FT3 mobile density is low-priority; FT1/F2 44px must not regress; markup edits would be change-all-11 (avoid — CSS suffices).
    - 04-RESEARCH.md §CNV-04 + §"Anti-Patterns" — do NOT flatten footer markup; per-page `.active` + 2 content strings are legitimate.
  </read_first>
  <action>
    Footer parity is CSS-only. Do NOT edit any footer markup — the per-page `.active` nav state and the two intentional content strings (recovery-service label; footer CTA heading) are legitimate and must stay (flattening them breaks the "you are here" affordance and is a documented anti-pattern). Because no markup is touched, this is NOT a change-all-11 edit.
    Append a new banner-commented stage to css/custom.css addressing FT3 mobile vertical density: when footer columns stack at <=575/390, tighten the inter-column and inter-widget vertical spacing using `--pfg-space-*` tokens (e.g. reduce stacked widget `margin-bottom`/`padding` to a smaller token step) so the footer is less tall on mobile. Preserve the gold 3px top-border. Do NOT reduce link/consent line-height or padding below the FT1/F2 44px tap-zone floor at css/custom.css:1240-1246 — keep tap targets >=44px.
    Use tokens only; no hardcoded px. Avoid net-new `!important` (footer rules win by specificity/source-order — CSS-only polish, not a vendor min-height fight).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && (git diff HEAD --name-only | grep -E '\.html$' && echo "CHECK: html changed (expected none in this plan)" || echo "OK: no html change"); test "$(grep -v '^[[:space:]]*/\*' css/custom.css | grep -v '^[[:space:]]*\*' | grep -c '!important')" -le "59" && echo "important <=59 OK"</automated>
  </verify>
  <done>
    Footer mobile density is tightened via tokens with the gold 3px top-border preserved and FT1/F2 tap-zones still >=44px; no footer markup edited in any of the 11 HTML files; filtered `!important` count stays <= 59 for this task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Imagery object-fit sizing (IMG-01) + T1 title-bar tablet tier</name>
  <files>css/custom.css</files>
  <read_first>
    - css/shortcode.css:4922-4966 — title-bar vendor source; `.pbmit-title-bar-wrapper, .pbmit-title-bar-content { min-height:550px }` at 4932-4934 (the rule T1 beats).
    - css/custom.css:985-1006 — existing §12.7 title-bar mobile override (@media max-width:575px → min-height:auto / content 200px). The new tier sits between 768 and this 575 collapse.
    - 04-RESEARCH.md §"Pattern 3" + §"Code Examples → Title-bar tablet tier" + §"Imagery sizing" + A4/A5 — exact selectors, the cited net-new `!important`, and the warning to target photo wrappers (not bare img) to avoid distorting logos/icons.
    - 04-UI-SPEC.md §IMG-01 + §T1 — presentation-only sizing; no re-encode; smaller min-height tier between 768 and 390 not regressing the >=390 collapse.
  </read_first>
  <action>
    IMG-01 (presentation only — no re-encode, assets untouched): append a banner-commented stage adding `object-fit:cover` (+ `width:100%; height:100%; display:block`) to the theme PHOTO wrappers so proportions never distort. Target the wrapper-scoped selectors (e.g. `.pbmit-featured-wrapper img`, `.pbmit-ihbox-img img`) — confirm the exact wrapper selectors against the live render first (A5); do NOT target bare `img` (would catch the logo/icons and distort them). Token/px-light, no hardcoded hex.
    T1 title-bar tablet tier: add `@media (max-width:768px)` overriding `.pbmit-title-bar-wrapper` and `.pbmit-title-bar-content` `min-height` to a smaller proportionate band (research suggests ~360px) plus reduced content padding, so 768/tablet no longer renders the full vendor 550px band. This must NOT regress the existing <=575px collapse (min-height:auto / 200px). The vendor rule (shortcode.css:4934) has equal specificity (0,2,0) and wins source-order on `min-height`, so this tier likely needs `!important` on `min-height` — add it with a Russian banner comment that CITES the beaten vendor rule (shortcode.css:4934) and phrase the comment WITHOUT the literal important-token (use the established phrasing «без important-флага» / «иначе vendor min-height выигрывает source-order»), so the grep budget is not inflated by comment prose. This is the single sanctioned net-new functional `!important` this phase — record the new honest count (60) with justification in the SUMMARY. If a non-`!important` selector wins the rendered box, prefer that and keep the count at 59.
    No re-encode, no markup edit, no JS edit.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'object-fit' css/custom.css; grep -c 'max-width:[[:space:]]*768px\|max-width: 768px' css/custom.css; test "$(grep -v '^[[:space:]]*/\*' css/custom.css | grep -v '^[[:space:]]*\*' | grep -c '!important')" -le "60" && echo "important <=60 OK (T1 +1 justified)"</automated>
  </verify>
  <done>
    css/custom.css contains `object-fit` rules scoped to theme photo wrappers (not bare img / not logos) and a `@media(max-width:768px)` title-bar tier reducing `.pbmit-title-bar-wrapper` min-height below 550px without regressing the <=575px collapse; any net-new `!important` (<=1, the T1 tier) cites shortcode.css:4934 in a Russian comment without the literal important-token; filtered `!important` count <= 60 and the new count is recorded with justification in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only presentation changes; no new inputs, links, or floating elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | vendor read-only invariant (shortcode.css) | medium | mitigate | T1 tier is an override in custom.css only; the vendor file is NOT edited; override wins by source-order + cited `!important` |
| T-04-05 | DoS | `!important` budget creep | low | mitigate | Filtered grep gate (<=60), comment phrasing avoids the literal important-token, new count recorded honestly |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No packages this phase; no registry — nothing to audit |
</threat_model>

<verification>
- No HTML file changed by this plan (footer parity is CSS-only; pricing markup belongs to plan 01).
- `git diff HEAD js/custom.js` empty (VER-04).
- Filtered `!important` count <= 60 (59 + at most the one cited T1 tier).
- DOM checks (footer border on 11, footer height @390, title-bar height @768 < 550, image render-vs-natural ratio, no h-scroll) run in the Wave-4 verification plan.
</verification>

<success_criteria>
- Footer visually unified across 11 pages (gold border preserved), tighter on mobile, tap-zones >=44px, no markup edit.
- Photo imagery uses object-fit (no distortion); logos/icons untouched.
- Title-bar shrinks below 550px at 768 without regressing the >=390 collapse; net-new `!important` <= 1, vendor-cited, count recorded.
</success_criteria>

<output>
Create `.planning/phases/04-conversion-blocks-imagery/04-03-SUMMARY.md` when done
</output>
