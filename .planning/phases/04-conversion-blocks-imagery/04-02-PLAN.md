---
phase: 04-conversion-blocks-imagery
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - css/custom.css
autonomous: true
requirements: [CNV-01, CNV-03]
must_haves:
  truths:
    - "Hero heading line-height ≥ font-size at 1440 and 1024 (no vertical clipping of the 2-line Cyrillic title)"
    - "Each hero slide shows exactly one CTA (already true — confirm, do not add a competing primary)"
    - "Hero trust/value-prop content reads above the fold without horizontal overflow at the 5 viewports"
    - "CNV-03 is satisfied by documented absence: no sticky mobile CTA exists; .pfg-whatsapp-float is the only floating element and overlaps nothing"
  artifacts:
    - "css/custom.css: hero visual-polish rules (extending §9.7 lh policy) if a rendered gap is found — token-driven, no copy change"
    - "04-02-SUMMARY.md: explicit CNV-03 resolution-by-absence record (sticky-CTA absent, float collision-free, js byte-identical)"
  key_links:
    - "Hero heading lh inherits --pfg-lh-display (1.15) at vendor specificity 0,2,0, winning by source-order — no !important"
    - "Hero copy (index.html:170/178/201/209/232/240) stays byte-identical (VER-04) — CNV-01 is polish, NOT retext"
---

<objective>
Polish the existing index hero (CNV-01) and resolve CNV-03 by documented absence. CNV-01 = confirm/extend the line-height policy so the 2-line Cyrillic title never clips, confirm exactly one CTA per slide (already true), and surface existing trust/value-prop above the fold — WITHOUT rewriting any hero copy (VER-04). CNV-03 = record that no sticky mobile CTA exists, so the anticipated sticky-CTA ↔ `.pfg-whatsapp-float` collision physically does not exist; no JS edit.

Purpose: the hero is the top conversion surface; a clipped headline or competing CTAs hurt the first impression. CNV-03 must be honestly closed, not fabricated.
Output: hero visual-polish rules in css/custom.css (only if a rendered defect is found) + a written CNV-03 resolution-by-absence in the SUMMARY. js/custom.js byte-identical.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-conversion-blocks-imagery/04-CONTEXT.md
@.planning/phases/04-conversion-blocks-imagery/04-RESEARCH.md
@.planning/phases/04-conversion-blocks-imagery/04-UI-SPEC.md

# Live source the executor must read before editing:
@index.html
@css/custom.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm/extend hero visual polish (CNV-01) — no copy change</name>
  <files>css/custom.css</files>
  <read_first>
    - index.html:160-258 — the 3-slide Swiper hero. Note: each slide has exactly ONE CTA («Наши услуги» / «Подробнее» / «Подробнее»), and a `.pbmit-slider-desc` trust/value line. NOTE the pre-existing markup typo at index.html:169-171 (opens `<h2 class="pbmit-slider-title">`, closes `</h1>`) — DO NOT silently rewrite it (see 04-RESEARCH Pitfall 2); flag it in the SUMMARY only.
    - css/custom.css:523-542 — the Phase-2 hero line-height policy (`.pbmit-slider-one .pbmit-slider-title` and the tween title both set `line-height:var(--pfg-lh-display)` at vendor specificity, winning by source-order, no `!important`).
    - 04-UI-SPEC.md §CNV-01 — value-prop kept byte-identical, exactly one primary CTA, trust above the fold, HE1/G2 lh ≥ fs, HE2 height is a visual note (perf is NOT a gate).
  </read_first>
  <action>
    This is visual polish ONLY. Hero copy is read-only (VER-04): do NOT change any text at index.html:170/178/201/209/232/240, and do NOT retext the CTA to «Получить консультацию» (that string is NOT the existing hero CTA — 04-RESEARCH A1/Pitfall 1).
    First confirm the Phase-2 lh policy already holds — read css/custom.css:523-542. The hero title and tween title already inherit `--pfg-lh-display` (1.15) so lh ≥ fs. If the Wave-4 DOM check (or your own read) shows lh ≥ fs at 1440/1024, add NO new rule — record "confirmed, no change needed" in the SUMMARY.
    Only if a genuine rendered defect is found (e.g. headline still clips, or trust line is pushed below the fold) add a minimal token-driven rule to css/custom.css (new banner-commented stage), targeting the vendor hero selectors at matching specificity to win by source-order. Permitted polish: heading line-height/margin, `.pbmit-slider-desc` spacing/visibility above the fold, ensuring exactly one prominent CTA per slide. Do NOT add a second/competing primary CTA. Do NOT touch the Swiper/GSAP brand animation, `swiper-*`, `data-aos*`, or `transform-*` hooks.
    No hardcoded hex/px (use `--pfg-*` tokens). No net-new `!important`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && git diff HEAD --quiet -- index.html && echo "index.html copy untouched OK"; test "$(grep -v '^[[:space:]]*/\*' css/custom.css | grep -v '^[[:space:]]*\*' | grep -c '!important')" -le "59"</automated>
  </verify>
  <done>
    Hero headline line-height ≥ font-size is confirmed (or a token-driven, no-`!important` polish rule was added to fix a real rendered gap); index.html has NO copy change (`git diff HEAD -- index.html` empty unless a deliberate, flagged a11y fix was made); exactly one CTA per slide preserved; filtered `!important` ≤ 59; Swiper/GSAP hooks untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve CNV-03 by documented absence (no JS, no sticky CTA)</name>
  <files>css/custom.css</files>
  <read_first>
    - css/custom.css:24-40 — `.pfg-whatsapp-float` (the ONLY floating/sticky element, `position:fixed`, `z-index:var(--pfg-z-float)`).
    - 04-CONTEXT.md §"Hero + sticky-CTA коллизия" + 04-UI-SPEC.md §CNV-03 — CNV-03 is satisfied by documenting that no sticky mobile CTA exists; the 01-IMPL "JS spike" line is superseded.
    - 04-RESEARCH.md §CNV-03 + §"State of the Art" — grep confirms only `.pfg-whatsapp-float` + vendor off-canvas are `fixed`; zero sticky/mobile-CTA classes.
  </read_first>
  <action>
    Do NOT create a sticky mobile CTA, and do NOT edit js/custom.js. CNV-03 is closed by documented absence.
    Verify the absence yourself: grep the repo for any `sticky`/`fixed` CTA bar in markup and CSS, and confirm the ONLY `position:fixed` floating element is `.pfg-whatsapp-float` (plus the vendor off-canvas menu). Confirm `git diff HEAD js/custom.js` is empty.
    In the 04-02-SUMMARY.md, record the CNV-03 resolution explicitly: (a) no sticky mobile CTA exists on the site; (b) the anticipated sticky-CTA ↔ `.pfg-whatsapp-float` collision physically cannot occur; (c) `.pfg-whatsapp-float` is the sole floating element and overlaps nothing at 390/360; (d) site-wide CTA hierarchy (one prominent primary per screen) is already delivered through Phase-2 tokens + Phase-3 button hierarchy + the new pricing block; (e) js/custom.js is byte-identical (VER-04). No code change is required for CNV-03.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && git diff HEAD --quiet -- js/custom.js && echo "js/custom.js byte-identical OK"; grep -rn 'position:[[:space:]]*fixed\|position: fixed' css/custom.css</automated>
  </verify>
  <done>
    `git diff HEAD js/custom.js` is empty; the only project-authored `position:fixed` floating element in css/custom.css is `.pfg-whatsapp-float`; no sticky CTA markup added anywhere; CNV-03 resolution-by-absence is written into 04-02-SUMMARY.md with the five points above.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds no new inputs, links, or floating elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | js/custom.js (VER-04 integrity) | medium | mitigate | Plan makes no JS edit; verified by empty `git diff HEAD js/custom.js` |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No packages this phase; no registry — nothing to audit |
</threat_model>

<verification>
- `git diff HEAD js/custom.js` empty (VER-04).
- index.html copy byte-identical (unless a flagged, deliberate a11y fix is made and recorded).
- Filtered `!important` ≤ 59.
- Hero lh ≥ fs and one-CTA-per-slide are DOM-confirmed in the Wave-4 verification plan.
</verification>

<success_criteria>
- Hero headline never clips (lh ≥ fs at 1440/1024); exactly one CTA per slide; trust/value-prop above the fold — all without any copy change.
- CNV-03 closed by documented absence; js/custom.js byte-identical.
</success_criteria>

<output>
Create `.planning/phases/04-conversion-blocks-imagery/04-02-SUMMARY.md` when done
</output>
