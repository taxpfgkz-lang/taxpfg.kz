---
phase: 02-design-tokens-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/custom.css
autonomous: true
requirements: [TOK-01, TOK-02, TOK-03]
must_haves:
  truths:
    - "The :root block in custom.css declares a complete --pfg-space-* 4px scale (4/8/12/16/24/32/48/64/96)"
    - "The :root block declares a fluid clamp() type scale (--pfg-fs-*) ratio ~1.25 and line-height tokens (--pfg-lh-*)"
    - "The single hardcoded z-index (whatsapp-float 9999) is expressed as a --pfg-z-float token with the float rule referencing it"
    - "Every existing --pfg-* token (custom.css:285-301) stays byte-identical — additive only, no rename"
  artifacts:
    - "css/custom.css :root with new --pfg-space-1..24, --pfg-fs-body/-label/-h2/-h3/-h4/-h5/-h6/-display, --pfg-lh-tight/-body/-display, --pfg-z-float"
  key_links:
    - ".pfg-whatsapp-float z-index now reads var(--pfg-z-float) — proves the token has a real consumer, not a dead declaration"
    - "No var(--pfg-*) reference anywhere in custom.css resolves to initial/inherit after the edit (no renamed token)"
---

<objective>
Lay the design-token foundation for Phase 2 by extending the existing `:root` block in `css/custom.css` (currently `custom.css:284-302`) with three additive token groups: a 4px spacing scale (`--pfg-space-*`), a fluid `clamp()` type scale (`--pfg-fs-*` + `--pfg-lh-*`), and a single z-index token (`--pfg-z-float`) for the one hardcoded `z-index:9999` (whatsapp-float). This is the consolidation half of TOK-01 plus the declaration half of TOK-02 (spacing scale) and TOK-03 (type scale).

Purpose: Components (Phase 3) and conversion blocks (Phase 4) cannot compose until this token sheet lands. Everything downstream inherits these tokens via `var()`.

Output: An extended `:root` token sheet. No selectors are restyled in this plan — declaration only. Application happens in 02-02.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-tokens-primitives/02-UI-SPEC.md
@.planning/phases/02-design-tokens-primitives/02-RESEARCH.md
@.planning/phases/02-design-tokens-primitives/02-CONTEXT.md
@css/custom.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend :root with spacing + type + z-index tokens (additive)</name>
  <files>css/custom.css</files>
  <read_first>
    - css/custom.css:284-302 (existing :root --pfg-* block — the consolidation base; KEEP verbatim)
    - css/custom.css:22-32 (the .pfg-whatsapp-float rule carrying z-index:9999 — the single z-index consumer)
    - css/base.css:21-49 (vendor --pbmit-* tokens; --pbmit-body-typography-line-height = source of body lh 1.6)
    - css/base.css:223-249 (vendor fixed-px heading scale h1 58/65 ... h6 22 — the desktop maxes the clamp() tops must match so desktop stays non-regressive)
    - 02-RESEARCH.md "Recommended :root Structure" + Open Question 2 (z-index)
  </read_first>
  <action>
    Inside the EXISTING `:root` block in css/custom.css (custom.css:284-302), append three new, clearly-commented token groups AFTER the existing motion tokens (`--pfg-tf` on the last line). Do NOT rename, reorder, or alter any existing token (`--pfg-ink` through `--pfg-tf`) — additive only, per the "extend, do not rename" decision (02-RESEARCH Runtime State caveat: a rename silently breaks every var() consumer).

    Group A — spacing scale (TOK-02 declaration), all multiples of 4, matching the 02-UI-SPEC table: --pfg-space-1 = 4px, --pfg-space-2 = 8px, --pfg-space-3 = 12px, --pfg-space-4 = 16px, --pfg-space-6 = 24px, --pfg-space-8 = 32px, --pfg-space-12 = 48px, --pfg-space-16 = 64px, --pfg-space-24 = 96px. Add a Russian banner comment naming the stage and citing TOK-02 / 4px scale.

    Group B — fluid type scale (TOK-03 declaration), ratio ~1.25, base body 16-18px. Each heading clamp() MAX must equal the current vendor desktop px (base.css:223-249) so 1440 desktop renders byte-identical; the MIN provides mobile fluidity. Recommended values (Claude's discretion within ratio 1.25 / base 16-18, per 02-CONTEXT): --pfg-fs-body = clamp(1rem, 0.95rem + 0.25vw, 1.125rem) /* 16->18 */; --pfg-fs-label = 15px /* keeps vendor --pbmit-btn-typography-font-size */; --pfg-fs-h6 = clamp(1.125rem, 1rem + 0.5vw, 1.375rem) /* ->22 */; --pfg-fs-h5 = clamp(1.25rem, 1.05rem + 0.9vw, 1.75rem) /* ->28 */; --pfg-fs-h4 = clamp(1.375rem, 1.05rem + 1.2vw, 2.125rem) /* ->34 */; --pfg-fs-h3 = clamp(1.5rem, 1.1rem + 1.8vw, 2.5rem) /* ->40 */; --pfg-fs-h2 = clamp(1.75rem, 1.2rem + 2.4vw, 3rem) /* ->48 */; --pfg-fs-display = clamp(2.125rem, 1.2rem + 4vw, 3.625rem) /* ->58, matches h1 */. Line-height tokens: --pfg-lh-tight = 1.2, --pfg-lh-body = 1.6, --pfg-lh-display = 1.15. Russian banner comment citing TOK-03, ratio 1.25, and that maxes mirror base.css vendor px to keep desktop unchanged.

    Group C — z-index token (TOK-01 completeness). There is exactly ONE hardcoded z-index in custom.css (z-index:9999 on .pfg-whatsapp-float, custom.css:28). Declare --pfg-z-float = 9999 and change that one declaration to z-index: var(--pfg-z-float). Do NOT invent a multi-level z-index scale — there is only one real consumer (02-RESEARCH Open Question 2).

    No `!important` is added anywhere in this task (pure :root declarations + one var() swap). No `@layer`. custom.css remains the last stylesheet.
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && grep -c -- '--pfg-space-' css/custom.css && grep -c -- '--pfg-fs-' css/custom.css && grep -n 'var(--pfg-z-float)' css/custom.css && grep -c '!important' css/custom.css</automated>
  </verify>
  <acceptance_criteria>
    - source: css/custom.css :root contains all nine spacing tokens; grep confirms `--pfg-space-4` value is `16px` and `--pfg-space-6` value is `24px`
    - source: css/custom.css :root contains `--pfg-fs-body`, `--pfg-fs-h2`, `--pfg-fs-display`, `--pfg-lh-body`, `--pfg-lh-display`
    - source: each `--pfg-fs-h{2..6}`/`--pfg-fs-display` clamp() MAX equals the matching base.css vendor px (h2=48px=3rem, h1/display=58px=3.625rem) so desktop is non-regressive
    - source: `.pfg-whatsapp-float` rule now reads `z-index: var(--pfg-z-float);` and `--pfg-z-float:9999` is declared
    - source: every original token `--pfg-ink` … `--pfg-tf` (custom.css:285-301) is unchanged byte-for-byte (no rename, no reorder)
    - gate: `grep -c '!important' css/custom.css` returns 59 (unchanged — this plan adds zero !important)
  </acceptance_criteria>
  <done>The :root token sheet is extended with spacing, fluid type, and z-index tokens; existing tokens untouched; whatsapp-float references --pfg-z-float; !important count stays at 59.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none server-side) | Static, backend-less, no auth/session/storage. The only boundary is CSS source → rendered browser DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | css/custom.css token edit | low | mitigate | Additive-only :root edit; existing tokens byte-identical; no selector/behavior change. js/custom.js untouched. |
| T-02-02 | Information Disclosure | CSS url()/external request | low | accept | This plan adds no `url()` and no external host reference — pure custom-property declarations. Existing inline-SVG data-URI pattern (custom.css:86) untouched. |

No package installs in this phase (no npm/pip/cargo) → no supply-chain (T-SC) threat applies.
</threat_model>

<verification>
- `grep -c '!important' css/custom.css` returns 59 (no net-new).
- All new tokens are declared inside the single existing `:root`; no second `:root` block introduced.
- custom.css remains the last stylesheet referenced in the HTML `<head>` (load order unchanged — markup not touched).
</verification>

<success_criteria>
The token foundation (spacing + fluid type + z-index) is declared and ready for application in 02-02. Existing token consumers continue to resolve. !important budget unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-design-tokens-primitives/02-01-SUMMARY.md` when done.
</output>
