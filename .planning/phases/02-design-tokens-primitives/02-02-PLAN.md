---
phase: 02-design-tokens-primitives
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - css/custom.css
autonomous: true
requirements: [TOK-02, TOK-03, VIS-02, VIS-03]
must_haves:
  truths:
    - "Card padding (custom.css:66 .pfg-card and :839 .pbmit-element-service-style-2 .pbmit-box-content-wrap) is snapped from 28px to a --pfg-space token (24) — no longer literal 28px"
    - "Mobile .form-control font-size is >=16px (iOS focus-zoom suppressed) via the existing .pfg-form hook, no !important added"
    - "No heading renders with line-height < font-size at any breakpoint (HE1/T2/G2 line-height policy normalized via type tokens)"
    - "Headings use text-wrap: balance and body prose uses text-wrap: pretty (orphan/widow guard); per-word Russian preposition non-breaking is explicitly DEFERRED (needs markup/JS)"
    - "Gold #ecab23 is never used as body text; gold-as-text stays --pfg-gold-ink #7a560a; axe color-contrast = 0 violations (== AUD-01 floor)"
    - "No horizontal scroll at 1440/1024/768/390/360; a11y >= 95 per page; JS behaviors (menu/WhatsApp/form/marquee/slider/reduced-motion) byte/behavior-identical (VER-04)"
  artifacts:
    - "css/custom.css: token-applied paddings, font-size on .pfg-form .form-control, heading line-height policy, text-wrap rules"
  key_links:
    - ".pfg-form .form-control font-size:16px wins by specificity 0,2,0 over vendor .form-control 0,1,0 (style.css:2641) WITHOUT !important"
    - "type tokens from 02-01 are consumed via var() by the applied rules — declaration and application connect"
---

<objective>
Apply the tokens declared in 02-01 to exactly the AUD-02-flagged findings, then prove no regression. This plan lands TOK-02 (spacing snap), TOK-03 (type scale application + line-height policy + Russian wrapping + F1 input fix), VIS-02 (documented alignment drift only), and VIS-03 (contrast enforcement / no-regression guard). It closes with the durable DOM-measured Playwright + axe verification and the VER-04 JS smoke.

Scope discipline (locked in 02-CONTEXT / 02-RESEARCH): snap-to-nearest ONLY the documented drift (card 28px x2, optionally documented title-bar/ihbox), do NOT rewrite paddings that already read correctly, do NOT restructure the vendor grid, do NOT grow the !important budget (ceiling 59), do NOT touch js/custom.js.

Purpose: Make the declared tokens real on the page where AUD-02 found problems, while guaranteeing the Phase-1 regression floor holds.

Output: Token-applied custom.css + a verification record (DOM measurements, axe results, JS smoke) in the SUMMARY.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-tokens-primitives/02-UI-SPEC.md
@.planning/phases/02-design-tokens-primitives/02-RESEARCH.md
@.planning/phases/02-design-tokens-primitives/02-CONTEXT.md
@.planning/phases/02-design-tokens-primitives/02-01-SUMMARY.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md
@css/custom.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply spacing snap (C1) + F1 input font-size + documented alignment drift (TOK-02, VIS-02)</name>
  <files>css/custom.css</files>
  <read_first>
    - css/custom.css:62-90 (.pfg-card primitive, padding:28px at :66)
    - css/custom.css:835-845 (.pbmit-element-service-style-2 .pbmit-box-content-wrap padding:28px at :839)
    - css/custom.css:494-509 (.pfg-form .form-control hook — add font-size here, no !important)
    - css/style.css:2635-2641 (vendor .form-control{font-size:15px} — the F1 source, READ-ONLY)
    - css/custom.css:284-302 (the --pfg-space-* tokens declared in 02-01)
    - css/custom.css:925-935 (existing title-bar 32px override — reference point for P2)
    - 01-AUDIT.md C1 / F1 / P2 findings; 02-UI-SPEC "AUD-02 Findings Phase 2 Fixes" table
  </read_first>
  <action>
    1. C1 card padding snap (TOK-02): replace the literal `padding:28px` at custom.css:66 (.pfg-card) and at custom.css:839 (.pbmit-element-service-style-2 .pbmit-box-content-wrap) with `var(--pfg-space-6)` (24px) — snap-to-nearest preserving rhythm per 02-CONTEXT. Both are own-rule paddings without `!important`, so swapping needs no `!important`. Append a short Russian inline comment on each citing AUD-02 C1 and the 28->24 snap. Do NOT touch any other padding value; paddings that already read correctly stay untouched (02-RESEARCH Pitfall 2).

    2. F1 input font-size (TOK-03 application): add `font-size:16px;` to the existing `.pfg-form .form-control, .pfg-form .form-select, .pfg-form textarea.form-control` block (custom.css:496-498). This wins by specificity 0,2,0 over vendor `.form-control` 0,1,0 (style.css:2641) and by source-order, so NO `!important` is needed (02-RESEARCH Pitfall 1). Russian comment: suppresses iOS<16 focus-zoom (F1); desktop visually unchanged (15->16px). Verified during planning: all contacts.html inputs sit under `<form class="pfg-form">` (the only page with a form), so the override reaches them.

    3. VIS-02 alignment drift: address ONLY drift documented in 01-AUDIT.md (P2 title-bar at 768px is partially spacing). If a documented drift is a pure padding snap, apply via a --pfg-space token at the SAME specificity the existing rule uses (the title-bar override already lives at custom.css:925-935). If a fix would require restructuring the vendor grid, leave it and record as out-of-scope in the SUMMARY (locked: do not restructure vendor grid). Do NOT add new `!important`; if a section-padding is NOT in the AUD-02 fix list, leave it untouched.
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && (grep -nE 'padding:\s*28px' css/custom.css || echo OK_NO_LITERAL_28) && grep -nE 'font-size:\s*16px' css/custom.css && grep -c '!important' css/custom.css</automated>
  </verify>
  <acceptance_criteria>
    - source: no literal `padding:28px` remains at custom.css:66 or :839 — both read `var(--pfg-space-6)`
    - source: `.pfg-form .form-control` block (custom.css:496-498) now declares `font-size:16px;` with no `!important`
    - DOM-measured @ 390 and 360: `getComputedStyle(document.querySelector('.form-control')).fontSize` >= 16px on contacts.html
    - DOM-measured @ all 5 widths: `getComputedStyle(document.querySelector('.pfg-card')).paddingTop` == 24px (snapped, not 28px)
    - gate: `grep -c '!important' css/custom.css` returns 59 (no net-new !important)
  </acceptance_criteria>
  <done>Card padding snapped to a token (24px) in both locations; mobile inputs are >=16px via the .pfg-form hook with no new !important; documented alignment drift addressed or recorded out-of-scope; budget stays 59.</done>
</task>

<task type="auto">
  <name>Task 2: Apply fluid type scale + line-height policy + Russian wrapping (TOK-03), guard contrast (VIS-03)</name>
  <files>css/custom.css</files>
  <read_first>
    - css/custom.css:284-302 (--pfg-fs-* / --pfg-lh-* tokens declared in 02-01)
    - css/custom.css:62-65 (.pfg-lead / .pfg-prose body type), :220-223 (.pfg-card h3 line-height 1.3)
    - css/custom.css:181-183 (existing mobile slider-title clamp override), :471-477 (hero slider-title rules), :925-935 (title-bar override)
    - css/base.css:223-249 (vendor bare h1-h6 — already lh>=fs; do NOT re-fight these)
    - css/custom.css:332-345 (scoped prefers-reduced-motion block — do not universalize)
    - css/custom.css:289 (--pfg-gold-ink), :122 (body color already darkened to AA), :359 (eyebrow gold-ink application)
    - 02-RESEARCH Pitfall 3 (hero specificity) + Pitfall 4 (Russian preposition CSS limit) + Open Question 1/3
  </read_first>
  <action>
    1. Line-height policy (TOK-03, fixes HE1/T2/G2): the only real lh<fs offender is the hero `.pbmit-slider-title` (vendor 170/150 in shortcode.css); bare h1-h6 already satisfy lh>=fs (base.css), so do NOT add competing bare-heading rules. Apply a line-height floor so NO clamped heading sits below font-size: ensure the hero/title-bar overrides carry `line-height: var(--pfg-lh-display)` (1.15) — at the SAME vendor-selector specificity the existing overrides use (`.pbmit-slider-one .pbmit-slider-title`, `.pbmit-title-bar-wrapper .pbmit-tbar-title`). Phase 2 owns the LINE-HEIGHT POLICY only; hero font-size height/LCP normalization is Phase 4 (locked, 02-RESEARCH A4 / Open Question 3). Reuse the existing `!important` already present on those vendor overrides — do NOT add new ones.

    2. Fluid type application (TOK-03): wire the body/prose roles to the fluid tokens where a `.pfg-*` rule already owns the size — e.g. `.pfg-lead`/`.pfg-prose` body sizing may reference `var(--pfg-fs-body)` and `var(--pfg-lh-body)`. Do NOT add blanket `h2{font-size:clamp()}` rules that would fight vendor selectors (Pitfall 3); apply tokens only where a project rule already sets size, keeping desktop visually identical (clamp maxes mirror vendor px from 02-01).

    3. Russian wrapping (TOK-03, locked): add `text-wrap: balance` to project heading rules (`.pfg-card h3`, `.pfg-section` headings, `.pbmit-*-title` overrides already owned) and `text-wrap: pretty` to body prose (`.pfg-prose p`, `.pfg-lead`). These degrade gracefully (~88% support). EXPLICITLY DEFERRED and recorded as a known limitation in the SUMMARY: per-word non-breaking of short Russian prepositions/conjunctions (в, и, с, к, на, по, от) — CSS alone cannot force a specific word to not end a line; that needs `&nbsp;` in read-only markup or a JS pass (out of scope this phase, 02-RESEARCH Open Question 1 / Pitfall 4). Do NOT promise CSS-only preposition non-breaking.

    4. VIS-03 contrast guard (enforcement, not redesign): confirm no new rule introduces gold `#ecab23` as body/normal text on a light surface. Gold-as-text stays `--pfg-gold-ink` `#7a560a`. Gold fill `#ecab23` stays untouched. This is a no-regression guard against the AUD-01 axe baseline (0 contrast violations) — verified by axe in Task 3.

    No new `!important`. No `@layer`. New comments in Russian, justifying the vendor behavior/WCAG criterion.
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && grep -nE 'text-wrap:\s*(balance|pretty)' css/custom.css && grep -nE 'var\(--pfg-(fs|lh)-' css/custom.css && (grep -nE '#ecab23' css/custom.css | grep -iE '\bcolor:' || echo OK_NO_GOLD_BODY_TEXT) && grep -c '!important' css/custom.css</automated>
  </verify>
  <acceptance_criteria>
    - source: `text-wrap: balance` present on at least one project heading rule and `text-wrap: pretty` on at least one body-prose rule
    - source: at least one `.pfg-*` size rule references `var(--pfg-fs-*)` / `var(--pfg-lh-*)` (declaration→application link proven)
    - source: hero/title-bar overrides carry a line-height tied to `--pfg-lh-display` (1.15) so the heading line-height floor is enforced at existing vendor specificity
    - source: no rule sets `color:#ecab23` on body/normal text (gold-as-text stays `--pfg-gold-ink`)
    - DOM-measured @ 1440: every visible heading `lineHeight >= fontSize` (no lh<fs); hero font-size unchanged from baseline (Phase 2 = lh policy only, not hero height)
    - DOM-measured @ 1440 vs 390: a clamped body/prose `fontSize` differs (fluid, not constant) while staying within the clamp min/max
    - gate: `grep -c '!important' css/custom.css` returns 59 (no net-new !important)
  </acceptance_criteria>
  <done>Heading line-height policy enforced (no lh<fs) via existing-specificity overrides; fluid type + line-height tokens applied where project rules own size; text-wrap balance/pretty added; preposition non-breaking recorded as DEFERRED; no gold body text; budget stays 59.</done>
</task>

<task type="auto">
  <name>Task 3: Verify against AUD-01 floor — DOM-measured Playwright + axe + VER-04 JS smoke</name>
  <files>css/custom.css</files>
  <read_first>
    - 02-RESEARCH "Verification Mechanics (Q7)" — exact DOM assertions + durable channel
    - .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md — AUD-01 per-page a11y floor (min 95) + axe = 0
    - 02-CONTEXT <specifics> — JS smoke list (menu / WhatsApp float / lead-form→WhatsApp / marquee / slider / reduced-motion)
  </read_first>
  <action>
    Run the durable Phase-1 verification channel and record results in the SUMMARY:

    1. Serve: `python -m http.server 8080` from repo root (so relative css/font/Maps paths resolve over HTTP).
    2. Playwright DOM-measure @ 1440 / 1024 / 768 / 390 / 360 across the affected pages (at minimum index.html, contacts.html, and one inner content page with a title-bar). Trust the RENDERED BOX, not CSS text. Assert: (a) `.pfg-card` paddingTop == 24px at all widths; (b) `.form-control` fontSize >= 16px @ 390/360 on contacts.html; (c) every heading lineHeight >= fontSize @ 1440; (d) a clamped body/prose fontSize scales between 1440 and 390; (e) `document.documentElement.scrollWidth <= clientWidth` at all 5 widths (no new horizontal overflow); (f) interactive tap targets still >=44px (unchanged).
    3. axe scan (via npx @axe-core/playwright) on all 11 pages for tags wcag2a/wcag2aa/wcag21a/wcag21aa: color-contrast violations == 0 (== AUD-01 floor); total violations not above baseline.
    4. a11y floor: Lighthouse accessibility per page >= AUD-01 row value (min 95). Performance recorded but NOT a gate this milestone.
    5. VER-04 JS smoke (no JS edited this phase, so behavior must be byte/identical): mobile menu toggle opens/closes; WhatsApp float renders + links; lead-form on contacts.html composes the WhatsApp deep link with the same number/message; marquee runs at its tuned speed; Swiper slider advances; scoped reduced-motion still only kills decorative micro-interactions (brand Swiper/GSAP/marquee intact).
    6. Budget gate: `grep -c '!important' css/custom.css` <= 59.

    If Playwright browsers are missing, run `npx playwright install chromium` (one-time, no project change). Record every measured number in the SUMMARY; if any assertion fails, fix in custom.css (within the locked scope) and re-run before declaring done.
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && grep -c '!important' css/custom.css</automated>
    <human-check>SUMMARY records: DOM measurements at all 5 widths (card padding 24px, input >=16px, no lh<fs, no horizontal overflow), axe color-contrast = 0 across 11 pages, a11y >= 95 per page, and VER-04 JS smoke all behavior-identical.</human-check>
  </verify>
  <acceptance_criteria>
    - gate: `grep -c '!important' css/custom.css` <= 59
    - verified: axe color-contrast violations == 0 on all 11 pages (== AUD-01 floor); total violations not above baseline
    - verified: Lighthouse a11y >= 95 (or the AUD-01 per-page value) on every page
    - DOM-measured @ all 5 widths: `document.documentElement.scrollWidth <= clientWidth` (no new horizontal scroll)
    - verified (VER-04): menu / WhatsApp float / lead-form→WhatsApp number+message / marquee / slider / reduced-motion all behavior-identical to baseline (js/custom.js untouched)
  </acceptance_criteria>
  <done>All DOM measurements meet the AUD-01 floor, axe contrast = 0 across 11 pages, a11y >= baseline, JS smoke behavior-identical, and the !important budget is <=59 — recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none server-side) | Static, backend-less. The only boundary is CSS source → rendered browser DOM. The lead form composes a WhatsApp deep link client-side (js/custom.js, untouched this phase). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | "style" edit silently changing behavior/flow | medium | mitigate | Hard constraint visual-only; js/custom.js NOT edited; VER-04 JS smoke confirms form→WhatsApp number+message byte/behavior-identical. |
| T-02-04 | Information Disclosure | CSS url() external request leak | low | accept | No new `url()` / third-party host added; token application reuses existing inline-SVG data-URI pattern. |
| T-02-05 | Denial of Service (a11y regression) | contrast/overflow regression vs AUD-01 floor | medium | mitigate | axe color-contrast = 0 + Lighthouse a11y >= 95 + no-horizontal-overflow asserted at 5 widths before done. |

No package installs (no npm/pip/cargo) → no supply-chain (T-SC) threat applies this phase.
</threat_model>

<verification>
- DOM-measured Playwright @ 1440/1024/768/390/360 meets every assertion in Task 3.
- axe color-contrast == 0 on all 11 pages; Lighthouse a11y >= AUD-01 floor (min 95).
- VER-04 JS smoke: all behaviors byte/behavior-identical (no JS edited).
- `grep -c '!important' css/custom.css` <= 59.
- custom.css remains the last stylesheet; no `@layer`; markup untouched.
</verification>

<success_criteria>
The declared tokens are applied to the four AUD-02 findings (C1 card padding, F1 input zoom, HE1/T2/G2 line-height policy, P2 title-bar where it is pure spacing), Russian wrapping lands as balance/pretty with preposition non-breaking honestly deferred, contrast holds at the axe-0 floor, and DOM measurement + a11y + JS smoke prove no regression against the Phase-1 baseline.
</success_criteria>

<output>
Create `.planning/phases/02-design-tokens-primitives/02-02-SUMMARY.md` when done.
</output>