---
phase: 01-baseline-audit-ui-design-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md
autonomous: true
requirements: [AUD-03]
user_setup: []

must_haves:
  truths:
    - "Every !important in css/custom.css is listed with file:line and the vendor rule it beats (baseline count = 59 per AUD-03)"
    - "The do-not-touch theme class/attribute list (swiper-*, data-aos*, pbmit-*) is enumerated as the override-safety boundary"
    - "chart.js is documented as vendored-but-unused, removal deferred to v2/PERF-02"
  artifacts:
    - ".planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md"
  key_links:
    - "the 59 count is custom.css-only (project override budget), NOT vendor !important (RESEARCH Pitfall 2) — it becomes the Phase 2 net-new≈0 budget floor"
    - "do-not-touch namespaces are the boundary that keeps later phases from breaking theme JS/animation hooks"
---

<objective>
Catalog the `custom.css` ↔ vendor conflict surface: the full `!important` ledger (verified baseline = 59) with file:line and the vendor rule each one beats, plus the do-not-touch theme class/attribute list (`swiper-*`, `data-aos*`, `pbmit-*`). This is AUD-03.

Purpose: Give Phases 2–5 a precise override-accounting and a hard boundary list so they never break theme JS/animation hooks and so the `!important` net-new≈0 budget has a concrete floor.
Output: `01-CONFLICT-CATALOG.md`.

DOCUMENTATION phase: edits NO site code. This is static analysis (grep/read) of existing files — `css/custom.css`, `css/base.css`, `js/custom.js` are read-only here.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-RESEARCH.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-CONTEXT.md
@css/custom.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the !important ledger from custom.css with vendor-rule attribution</name>
  <files>.planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md</files>
  <read_first>
    - css/custom.css (the only file with the project !important budget)
    - .planning/phases/01-baseline-audit-ui-design-contract/01-RESEARCH.md (Q5 verified count=59; Pitfall 2 = count custom.css only, not vendor)
    - .planning/phases/01-baseline-audit-ui-design-contract/01-CONTEXT.md (AUD-03 decisions)
  </read_first>
  <action>
    Enumerate every `!important` occurrence in css/custom.css. Verify the count equals 59 (RESEARCH Q5; if the live count differs from 59, record the actual number and note the delta from the documented baseline — do not silently accept a mismatch). The count is custom.css-ONLY — do NOT count vendor `!important`s (RESEARCH Pitfall 2); vendor occurrences are context, not the project budget.

    Create 01-CONFLICT-CATALOG.md. Open with a header (milestone, date, statement that 59 is the project override budget floor for Phase 2's net-new≈0 rule). Then a "## !important ledger" table with one row per occurrence: # | custom.css line | selector + property | vendor rule it beats (file:line if cited in the adjacent Russian comment, else "—"). Many existing entries already cite the beaten vendor rule in their section comment (e.g. custom.css:651-652 cites responsive.css:145) — capture those citations. Where no citation exists, mark the vendor target as "uncited" so Phase 2 knows to confirm before touching.

    Add a one-line tally confirming the total (e.g. "Total: 59 — matches AUD-03 baseline").
  </action>
  <verify>
    <automated>grep -qiE 'important ledger' .planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md && grep -q '59' .planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md</automated>
  </verify>
  <acceptance_criteria>
    - 01-CONFLICT-CATALOG.md exists with an "!important ledger" table
    - The ledger lists every custom.css !important occurrence with its custom.css line and selector/property
    - The total tally is stated and reconciled against the 59 baseline (delta noted if not 59)
    - Where the adjacent comment cites a beaten vendor rule (file:line), that citation is captured; uncited ones are marked "uncited"
  </acceptance_criteria>
  <done>The full custom.css !important ledger is documented with file:line and vendor-rule attribution, total reconciled to the 59 baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Enumerate the do-not-touch namespaces and the unused-chart.js note</name>
  <files>.planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md</files>
  <read_first>
    - .planning/phases/01-baseline-audit-ui-design-contract/01-RESEARCH.md (Q4 chart.js unused; do-not-touch namespaces; Pattern: trust DOM)
    - .planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md (Hard Constraints table — off-canvas-safe, load-order)
    - js/scripts.js (theme JS hooks the namespaces map to — read only the init/selector regions)
  </read_first>
  <action>
    Append a "## Do-not-touch theme classes/attributes" section to 01-CONFLICT-CATALOG.md. List the protected namespaces with WHY each is off-limits and which behavior breaks if renamed/removed: `swiper-*` (Swiper slider + services marquee — theme JS binds by class; renaming kills the slider and the marquee speed fix in js/custom.js), `data-aos*` (AOS animate-on-scroll attributes — theme reads them on init), `pbmit-*` (GudFin theme structural/visual + JS hooks — project CSS targets these to override but never owns or renames them). Cross-reference the off-canvas-safe constraint (no filter/backdrop-filter/transform on ancestors of the mobile off-canvas menu) and the load-order constraint (custom.css last, custom.js last) from the UI-SPEC Hard Constraints table, since both are conflict-surface boundaries.

    Append a short "## Unused vendor: chart.js" note: chart.js is vendored but referenced by zero pages (RESEARCH Q4), contributes zero live bytes, and its removal is OUT OF SCOPE this milestone — deferred to v2/PERF-02. Record it so later phases do not "discover" it and act.

    State explicitly at the top of these sections that all listed files (css/*, js/scripts.js, etc. except custom.css/base.css/custom.js) are vendor read-only.
  </action>
  <verify>
    <automated>grep -qiE 'do-not-touch|do not touch' .planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md && grep -q 'swiper' .planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md && grep -q 'data-aos' .planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md && grep -qi 'chart.js' .planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md</automated>
  </verify>
  <acceptance_criteria>
    - A "Do-not-touch" section lists swiper-*, data-aos*, and pbmit-* with the breakage each rename/removal causes
    - The off-canvas-safe and load-order boundaries are cross-referenced
    - A chart.js note records it as vendored-but-unused, removal deferred to v2/PERF-02
    - The vendor-read-only boundary is stated explicitly
    - No site code file was modified (git status shows only .planning/ changes)
  </acceptance_criteria>
  <done>The do-not-touch namespace boundary and the unused-chart.js disposition are documented as the override-safety guardrails for Phases 2–5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-only static analysis | This plan only reads/greps existing files; no external input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering | accidental edit of vendor/site code while cataloging | low | mitigate | Plan is read-only; verify `git status` shows only .planning/ changes before completion. No installs, no network calls. |

No packages installed, no network-exposed surface, documents-only (RESEARCH Security Domain = N/A).
</threat_model>

<verification>
- 01-CONFLICT-CATALOG.md contains the !important ledger reconciled to 59, the do-not-touch namespaces, and the chart.js note
- `git status` shows changes only under .planning/ — no edits to css/*, js/*, or *.html
</verification>

<success_criteria>
The conflict surface (override budget + do-not-touch boundary + unused-lib disposition) is fully documented for AUD-03, with zero site-code changes.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-audit-ui-design-contract/01-02-SUMMARY.md` when done.
</output>
