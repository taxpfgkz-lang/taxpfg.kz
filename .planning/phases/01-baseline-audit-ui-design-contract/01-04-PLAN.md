---
phase: 01-baseline-audit-ui-design-contract
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - .planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md
  - .planning/phases/01-baseline-audit-ui-design-contract/01-IMPL-PLAN.md
autonomous: true
requirements: [AUD-04, AUD-05]
user_setup: []

must_haves:
  truths:
    - "The approved UI design contract (01-UI-SPEC.md) is verified complete against AUD-04: tokens, spacing scale, type scale, color+contrast, component states, hard-constraints all present"
    - "01-IMPL-PLAN.md states which files (custom.css/base.css/custom.js) change in Phases 2-5, why, and in what order"
    - "01-IMPL-PLAN.md explicitly marks pricing/FAQ/modal as net-new (not polish), target-size as accepted exception, and chart.js as out-of-scope"
  artifacts:
    - ".planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md (AUD-04 — verified/signed-off, gaps closed if any)"
    - ".planning/phases/01-baseline-audit-ui-design-contract/01-IMPL-PLAN.md (AUD-05)"
  key_links:
    - "AUD-04 is already drafted+approved in 01-UI-SPEC.md — this plan VERIFIES completeness against the requirement and closes gaps, it does not re-author the contract"
    - "IMPL-PLAN consumes the conflict catalog (01-02) and the visual-problem inventory (01-03) to sequence the file-change order"
    - "the !important net-new≈0 budget (floor 59 from 01-02) is carried into the IMPL-PLAN as a Phase-2 gate"
---

<objective>
Finalize the two contract artifacts: (1) verify the already-approved UI design contract (01-UI-SPEC.md) is complete against AUD-04 and close any gap; (2) write the implementation plan (01-IMPL-PLAN.md, AUD-05) stating which files change in Phases 2–5, why, and in what order — consuming the conflict catalog and visual-problem inventory.

Purpose: Lock the rules and the work-order before any code is written, so Phases 2–5 execute deterministically against a frozen contract.
Output: signed-off 01-UI-SPEC.md (AUD-04) + 01-IMPL-PLAN.md (AUD-05).

DOCUMENTATION phase: edits NO site code. The only writes are to the two .planning/ artifacts.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-RESEARCH.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-CONTEXT.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the UI design contract complete against AUD-04 and sign off</name>
  <files>.planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md (the approved contract — AUD-04 deliverable)
    - .planning/REQUIREMENTS.md (AUD-04 wording: tokens, spacing scale, type scale, color+contrast, states, component rules, hard-constraints)
    - .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md (axe contrast findings to reconcile against the contrast floor)
  </read_first>
  <action>
    01-UI-SPEC.md is the AUD-04 deliverable and is already drafted and approved — do NOT re-author it. Verify it is COMPLETE against the AUD-04 requirement checklist: (a) design tokens (ink/gold palette, radii, shadows, transitions, z-index namespaces), (b) spacing scale (4px `--pfg-space-*`), (c) type scale (clamp fluid, ratio ≈1.25, Russian wrapping rules), (d) color + contrast floor (WCAG AA, gold-as-text forbidden → gold-ink), (e) component state contract (hover/focus-visible/active/disabled; button primary/secondary/ghost; forms; cards; nav; FAQ/modal conditioned on instantiation), (f) hard-constraints (visual-only, vendor read-only, no @layer, focus-always, scoped-motion, no-build, load-order, off-canvas-safe, change-all-11, !important budget, comments-russian).

    Reconcile the contrast floor against the actual axe contrast violations recorded in 01-AUDIT.md: if axe surfaced any contrast violation the contract does not already account for, add a row/note to the contract's Color section (this is the only legitimate edit — closing a verified gap, not redesigning). If no gap, leave the contract content unchanged.

    Mark the "Open Questions to Resolve in This Phase" items in 01-UI-SPEC.md as resolved by pointing to where each was answered (font-loading + img inventory + pricing/FAQ/modal absence + chart.js → 01-RESEARCH Q1–Q4; target-size → 01-AUDIT). Tick the Checker Sign-Off dimensions and set Approval from pending to approved (with date), since the contract is verified complete. Do not weaken or remove any locked decision.
  </action>
  <verify>
    <automated>grep -qiE 'approv(ed|al)' .planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md && grep -qE 'Hard Constraints' .planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md && grep -qiE 'spacing scale' .planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - 01-UI-SPEC.md confirmed to contain all six AUD-04 areas (tokens, spacing, type, color+contrast, states, hard-constraints)
    - Contrast floor reconciled against axe findings in 01-AUDIT.md; any verified gap closed by a noted addition (no redesign)
    - Open-Questions items marked resolved with a pointer to where each was answered
    - Checker Sign-Off dimensions ticked and Approval set to approved with a date
    - No locked decision weakened or removed; no site code modified
  </acceptance_criteria>
  <done>01-UI-SPEC.md is verified complete against AUD-04 and signed off, with any axe-surfaced contrast gap closed.</done>
</task>

<task type="auto">
  <name>Task 2: Write the implementation plan (AUD-05) sequencing Phases 2-5</name>
  <files>.planning/phases/01-baseline-audit-ui-design-contract/01-IMPL-PLAN.md</files>
  <read_first>
    - .planning/phases/01-baseline-audit-ui-design-contract/01-CONFLICT-CATALOG.md (override budget + do-not-touch boundary from 01-02)
    - .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md (visual problems by block-type from 01-03 — the "what to fix")
    - .planning/ROADMAP.md (Phase 2-5 requirement mapping + recurring gate notes)
    - .planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md (Hard Constraints to honor)
  </read_first>
  <action>
    Create 01-IMPL-PLAN.md (AUD-05). Open with purpose + a statement that it consumes the conflict catalog (01-02), the visual-problem inventory (01-03), and the contract (01-UI-SPEC.md).

    Add a "## File-change order" section mapping each downstream phase to the files it edits and why, in dependency order: Phase 2 (Design Tokens + Primitives) → primarily css/custom.css `:root` token consolidation + spacing/type scale; css/base.css only if a vendor token must be referenced/overridden; why = root dependency everything inherits. Phase 3 (Components) → css/custom.css `.pfg-*` component rules + js/custom.js only for a11y/ARIA patches (no behavior change); why = needs tokens from P2. Phase 4 (Conversion Blocks + Imagery) → css/custom.css for hero/CTA/footer/imagery; js/custom.js for the sticky-CTA↔WhatsApp collision resolution; why = needs stable components. Phase 5 (A11y + Verification) → css/custom.css focus/motion polish + the verification run; why = final proof against the AUD-01 floor. For each phase list the specific files and the reason; honor the vendor-read-only set (only custom.css/base.css/custom.js editable).

    Add a "## Net-new vs polish" section: explicitly mark pricing (CNV-02), FAQ accordion (CMP-05), and modal (CMP-06) as NET-NEW features (the site has none today — RESEARCH Q3), so Phases 3–4 build them rather than "polish existing"; flag that CMP-06 only proceeds if Magnific Popup is actually instantiated (it is not today). Mark the mobile header search target-size flag as an accepted documented exception. Mark chart.js removal as OUT OF SCOPE (v2/PERF-02). Do NOT use scope-reducing language (no "v1/placeholder/static-for-now") — net-new features are to be delivered in full in their phase, not simplified.

    Add a "## Carried gates" section: the !important net-new≈0 budget (floor = the count reconciled in 01-02, baseline 59, each new one citing the vendor rule it beats); the recurring multi-breakpoint DOM-measured Playwright + axe-0-new + JS-smoke + change-all-11 grep gate (from ROADMAP P2-P5 notes); the AUD-01 baseline as the regression floor Phase 5 measures against.
  </action>
  <verify>
    <automated>grep -qiE 'file-change order' .planning/phases/01-baseline-audit-ui-design-contract/01-IMPL-PLAN.md && grep -qiE 'net-new' .planning/phases/01-baseline-audit-ui-design-contract/01-IMPL-PLAN.md && grep -qiE 'custom\.css' .planning/phases/01-baseline-audit-ui-design-contract/01-IMPL-PLAN.md && grep -qiE 'out of scope|out-of-scope' .planning/phases/01-baseline-audit-ui-design-contract/01-IMPL-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - 01-IMPL-PLAN.md exists with a file-change-order section mapping Phases 2-5 → files (custom.css/base.css/custom.js) + why, in dependency order
    - Pricing/FAQ/modal explicitly marked NET-NEW (not polish); CMP-06 conditioned on Magnific Popup instantiation
    - Target-size flag marked accepted exception; chart.js marked out-of-scope (v2/PERF-02)
    - A carried-gates section records the !important budget floor, the recurring verification gate, and the AUD-01 regression floor
    - No scope-reducing language used; no site code modified (git status shows only .planning/ changes)
  </acceptance_criteria>
  <done>01-IMPL-PLAN.md sequences the Phase 2-5 file changes with rationale, marks net-new vs out-of-scope items honestly, and carries the budget/verification gates forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-only synthesis | This plan only reads prior artifacts and writes two .planning/ documents; no external input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | weakening a locked contract decision while "verifying" | medium | mitigate | Task 1 forbids re-authoring; only a verified axe-gap closure or sign-off ticks are allowed. Locked decisions from CONTEXT.md are non-negotiable. |
| T-01-06 | Tampering | accidental site-code edit | low | mitigate | Plan writes only to .planning/; verify `git status` shows only .planning/ changes. |

Documents-only phase; no installs, no network-exposed surface (RESEARCH Security Domain = N/A).
</threat_model>

<verification>
- 01-UI-SPEC.md verified complete against AUD-04 and signed off (Approval = approved)
- 01-IMPL-PLAN.md has file-change order, net-new-vs-polish, and carried-gates sections
- `git status` shows changes only under .planning/ — no edits to css/*, js/*, or *.html
</verification>

<success_criteria>
The design contract is verified-complete and signed off (AUD-04) and the implementation plan sequences Phases 2-5 with honest scope and carried gates (AUD-05), with zero site-code changes.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-audit-ui-design-contract/01-04-SUMMARY.md` when done.
</output>
