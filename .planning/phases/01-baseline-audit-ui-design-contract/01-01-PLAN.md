---
phase: 01-baseline-audit-ui-design-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md
  - .planning/phases/01-baseline-audit-ui-design-contract/baseline/
autonomous: true
requirements: [AUD-01]
user_setup: []

must_haves:
  truths:
    - "Lighthouse perf + accessibility + CLS + LCP recorded for all 11 pages over http://localhost"
    - "axe-core WCAG rule violations recorded for all 11 pages"
    - "01-AUDIT.md opens with an 11-row per-page baseline table (a11y/perf/CLS/LCP) = the regression floor for AUD-01"
  artifacts:
    - ".planning/phases/01-baseline-audit-ui-design-contract/baseline/lighthouse/ (11 JSON + 11 HTML)"
    - ".planning/phases/01-baseline-audit-ui-design-contract/baseline/axe/ (11 JSON)"
    - ".planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md (baseline section)"
  key_links:
    - "baseline/ lives under .planning/ (not repo root) so it is NOT gitignored and persists as the committed regression floor"
    - "all audits run through http://localhost:8080 (never file://) so relative css/js + @import Google Fonts + Maps iframe resolve and metrics are accurate (RESEARCH Pitfall 4)"
---

<objective>
Capture the per-page Lighthouse + axe-core regression floor for all 11 pages of taxpfg.kz BEFORE any code changes. This is AUD-01 — the dependency gate that Phase 5 (A11Y-04, VER-01) is measured against.

Purpose: Produce a reproducible, committed numeric baseline (a11y / perf / CLS / LCP per page) plus raw axe WCAG violations, so later phases can prove "не хуже текущего".
Output: `baseline/lighthouse/` (JSON+HTML ×11), `baseline/axe/` (JSON ×11), and the per-page baseline table opening `01-AUDIT.md`.

This is a DOCUMENTATION phase: it edits NO site code (`css/custom.css`, `css/base.css`, `js/custom.js` stay byte-identical). The only writes are tool runs into `.planning/phases/01-baseline-audit-ui-design-contract/`.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-RESEARCH.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serve the site and run Lighthouse across all 11 pages</name>
  <files>.planning/phases/01-baseline-audit-ui-design-contract/baseline/lighthouse/</files>
  <read_first>
    - .planning/phases/01-baseline-audit-ui-design-contract/01-RESEARCH.md (Q6 tooling path + Code Examples; Pitfall 4 = never file://)
    - .planning/phases/01-baseline-audit-ui-design-contract/01-CONTEXT.md (audit tooling decisions)
  </read_first>
  <action>
    Start a local static server from the repo root with `python -m http.server 8080` in one shell (PowerShell host — do NOT use `&`-backgrounding; run it in its own shell via the Bash tool background mode or a separate invocation). Confirm it is reachable at http://localhost:8080/index.html.

    For each of the 11 pages — index.html, about.html, services.html, accounting.html, accounting-recovery.html, taxes.html, consulting.html, registration.html, contacts.html, privacy.html, 404.html — run `npx -y lighthouse@13.4.0 http://localhost:8080/<page> --output=json --output=html --output-path=.planning/phases/01-baseline-audit-ui-design-contract/baseline/lighthouse/<slug> --only-categories=performance,accessibility --chrome-flags="--headless=new"`, where `<slug>` is the page filename without `.html` (use `404` for 404.html). Pin Lighthouse to 13.4.0 for reproducibility (RESEARCH State-of-the-Art).

    Tools are external and npx-invoked — do NOT create a package.json, lockfile, or any build step in the repo (no-build hard constraint). Nothing is installed into the site tree.

    From each Lighthouse JSON extract for the table later: accessibility score, performance score, CLS (cumulative-layout-shift numericValue), LCP (largest-contentful-paint numericValue). Leave the JSON+HTML in place as the raw floor.
  </action>
  <verify>
    <automated>test $(ls .planning/phases/01-baseline-audit-ui-design-contract/baseline/lighthouse/*.report.json 2>/dev/null | wc -l) -eq 11</automated>
  </verify>
  <acceptance_criteria>
    - 11 Lighthouse JSON reports exist under baseline/lighthouse/ (one per page, named by slug)
    - 11 matching HTML reports exist
    - Each JSON contains non-null accessibility + performance scores and CLS + LCP numericValues (audits ran over http://localhost, not file://)
    - No package.json / lockfile / build artifact was created in the repo
  </acceptance_criteria>
  <done>All 11 pages have stored Lighthouse JSON+HTML with valid a11y/perf/CLS/LCP, captured over the local HTTP server.</done>
</task>

<task type="auto">
  <name>Task 2: Run axe-core WCAG scan across all 11 pages</name>
  <files>.planning/phases/01-baseline-audit-ui-design-contract/baseline/axe/</files>
  <read_first>
    - .planning/phases/01-baseline-audit-ui-design-contract/01-RESEARCH.md (axe-core via Playwright code example; Don't Hand-Roll = use axe, not custom WCAG math)
  </read_first>
  <action>
    With the static server still running on :8080, run an axe-core WCAG scan against each of the 11 page URLs. Prefer the existing Playwright MCP channel; if scripting is needed, use `@axe-core/playwright` 4.12.1 in an ephemeral npx-invoked script, or fall back to `npx -y @axe-core/cli@4.12.1 http://localhost:8080/<page>` per URL. Store one JSON of violations per page under baseline/axe/<slug>.json.

    Do NOT hand-roll contrast or WCAG math — axe handles compositing, opacity, gradients, and text-size thresholds correctly (RESEARCH Don't Hand-Roll). Capture the full `violations` array per page (id, impact, nodes, help) so the visual-audit plan (01-03) and the design-contract verification (01-04) can cite specific rule IDs.

    Record any axe violation that corresponds to the known mobile header search target-size flag, but do NOT treat it as new work — it is a previously accepted documented exception (RESEARCH Open Question 2). Note it for 01-03 to log as an exception unless it surfaces as a hard blocker.
  </action>
  <verify>
    <automated>test $(ls .planning/phases/01-baseline-audit-ui-design-contract/baseline/axe/*.json 2>/dev/null | wc -l) -eq 11</automated>
  </verify>
  <acceptance_criteria>
    - 11 axe JSON files exist under baseline/axe/ (one per page)
    - Each file contains a `violations` array (empty array is valid and means zero violations for that page)
    - Scans ran over http://localhost:8080, not file://
  </acceptance_criteria>
  <done>All 11 pages have a stored axe-core violations JSON captured over the local HTTP server.</done>
</task>

<task type="auto">
  <name>Task 3: Write the AUD-01 per-page baseline table into 01-AUDIT.md</name>
  <files>.planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md</files>
  <read_first>
    - .planning/phases/01-baseline-audit-ui-design-contract/baseline/lighthouse/ (the 11 JSON just produced)
    - .planning/phases/01-baseline-audit-ui-design-contract/baseline/axe/ (the 11 JSON just produced)
  </read_first>
  <action>
    Create 01-AUDIT.md. Open it with a short header naming the milestone, the audit date, the tool versions used (lighthouse 13.4.0, @axe-core/playwright 4.12.1, served via python http.server on :8080), and a one-line statement that these numbers are the committed regression floor for AUD-01 (the gate Phase 5 measures against).

    Then add a "## AUD-01 — Per-page baseline" table with exactly one row per page (11 rows), columns: Page | A11y score | Perf score | CLS | LCP (ms) | axe violations (count) | axe critical/serious IDs. Pull A11y/Perf/CLS/LCP from the Lighthouse JSON and the violation count + impactful rule IDs from the axe JSON. Use the real page filenames as row labels.

    Add a short "Floor note" line stating the lowest a11y score across the 11 pages (this is the hard floor any later phase must not drop below) and listing which pages, if any, already have axe violations.

    Leave a placeholder heading "## AUD-02 — Visual problems by block-type" with the text "Filled by plan 01-03" so the visual-audit plan appends there without restructuring. Comments/prose in artifacts may be Russian or English per Claude's Discretion (CONTEXT.md); keep table headers consistent.
  </action>
  <verify>
    <automated>grep -qE 'AUD-01' .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md && test $(grep -cE '\| *(index|about|services|accounting|accounting-recovery|taxes|consulting|registration|contacts|privacy|404)\.html *\|' .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md) -ge 11</automated>
  </verify>
  <acceptance_criteria>
    - 01-AUDIT.md exists with an "AUD-01" baseline section
    - The baseline table has 11 data rows, one per real page filename, with columns for A11y / Perf / CLS / LCP / axe-violation-count
    - A floor note records the minimum a11y score across the 11 pages
    - An "AUD-02 — Visual problems by block-type" placeholder heading is present for plan 01-03 to fill
    - No site code file was modified (git status shows only .planning/ changes)
  </acceptance_criteria>
  <done>01-AUDIT.md records the 11-page numeric regression floor with values sourced from the stored Lighthouse and axe JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx → public npm registry | Audit tools (lighthouse, axe-core, playwright) downloaded ephemerally at run time |
| local static server → localhost only | python http.server binds :8080 for headless Chrome; not exposed beyond loopback |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npx-fetched audit tooling (lighthouse@13.4.0, @axe-core/playwright@4.12.1) | low | accept | First-party Google/Microsoft/Deque packages, version-pinned, npx-ephemeral, nothing installed into the repo tree; legitimacy table in RESEARCH = all OK. No code or dependency persisted in the site. |
| T-01-01 | Information disclosure | local http.server :8080 | low | accept | Serves only the public static site to loopback for the duration of the run; no secrets in repo; stop the server when capture completes. |

No code, auth, data, or network-exposed surface is created (RESEARCH Security Domain = N/A). Documents-only phase.
</threat_model>

<verification>
- All 11 Lighthouse JSON+HTML and 11 axe JSON exist under baseline/
- 01-AUDIT.md baseline table has 11 rows with real values
- `git status` shows changes only under .planning/ — no edits to css/*, js/*, or *.html
- baseline/ path confirmed NOT matched by .gitignore (it is under .planning/, not repo root)
</verification>

<success_criteria>
The committed regression floor (a11y/perf/CLS/LCP + axe violations) exists for all 11 pages and is summarized in the AUD-01 table, with zero site-code changes.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-audit-ui-design-contract/01-01-SUMMARY.md` when done.
</output>
