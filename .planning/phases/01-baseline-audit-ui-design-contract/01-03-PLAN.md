---
phase: 01-baseline-audit-ui-design-contract
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md
  - .planning/phases/01-baseline-audit-ui-design-contract/baseline/screenshots/
autonomous: true
requirements: [AUD-02]
user_setup: []

must_haves:
  truths:
    - "Visual problems are enumerated what/where/why, grouped by block-type (header/footer/hero/cards/forms/nav/title-bar), not duplicated 11×"
    - "Each finding records the viewport(s) it appears at among 1440/1024/768/390/360 and a DOM-measured value (not CSS source text)"
    - "Screenshots exist per block-type per breakpoint to evidence each finding"
  artifacts:
    - ".planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md (AUD-02 section filled)"
    - ".planning/phases/01-baseline-audit-ui-design-contract/baseline/screenshots/"
  key_links:
    - "block-type grouping (RESEARCH Pattern 1) avoids 11× duplication; shared chrome audited once, page-specific deltas noted"
    - "DOM-measured truth (RESEARCH Pattern 2) — measure the rendered box at each viewport, do not trust custom.css source text"
    - "absent features (pricing/FAQ/live modal) are recorded as ABSENT, never audited as present (RESEARCH Anti-Pattern)"
---

<objective>
Enumerate the site's visual problems with what/where/why across desktop/tablet/mobile, grouped by block-type and evidenced by DOM-measured Playwright screenshots at 5 viewports. This is AUD-02. Appends to the AUD-02 placeholder in 01-AUDIT.md created by plan 01-01.

Purpose: Give Phases 2–5 a precise, de-duplicated problem list (the "what to fix") tied to real rendered measurements and the axe findings from the baseline.
Output: The "AUD-02 — Visual problems by block-type" section of 01-AUDIT.md + screenshots under baseline/screenshots/.

DOCUMENTATION phase: edits NO site code. Measurement and reporting only.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-RESEARCH.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-CONTEXT.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture DOM-measured screenshots of each block-type at 5 viewports</name>
  <files>.planning/phases/01-baseline-audit-ui-design-contract/baseline/screenshots/</files>
  <read_first>
    - .planning/phases/01-baseline-audit-ui-design-contract/01-RESEARCH.md (Pattern 1 block-type grouping; Pattern 2 DOM-measured; Q3 absent features; Pitfall 5)
    - .planning/phases/01-baseline-audit-ui-design-contract/01-CONTEXT.md (5 viewports; group by block-type; representative pages = Claude's Discretion)
  </read_first>
  <action>
    With the static server on :8080 (start `python -m http.server 8080` if not already running), drive the existing Playwright MCP to load representative pages at each of the 5 viewports — 1440, 1024, 768, 390, 360 px width. Representative-page selection is Claude's Discretion (CONTEXT.md): use index.html for hero + slider + img-heavy blocks, one inner page (e.g. about.html or services.html) for the title-bar, contacts.html for the lead form + map, and any inner page for the shared header/footer/nav. Do NOT screenshot all 11 pages at all 5 viewports — capture each block-type once on its representative page plus any page-specific delta you observe.

    Capture screenshots per block-type per breakpoint into baseline/screenshots/ with descriptive names (e.g. header-390.png, footer-1440.png, hero-768.png, cards-1024.png, form-390.png, nav-offcanvas-360.png). For each block, record DOM-measured values via Playwright (boundingBox / computed box) rather than reading custom.css text (RESEARCH Pattern 2 + Pitfall 5): note things like element width vs viewport (horizontal-scroll check), tap-target size vs 44px floor, heading clipping/wrapping, spacing gaps. Keep a working notes list mapping each measurement to its block-type and viewport for Task 2.

    Note absent features explicitly: there is NO pricing block, NO FAQ accordion, and NO instantiated modal anywhere (RESEARCH Q3) — do not screenshot or audit them as if present.
  </action>
  <verify>
    <automated>test -d .planning/phases/01-baseline-audit-ui-design-contract/baseline/screenshots && test $(ls .planning/phases/01-baseline-audit-ui-design-contract/baseline/screenshots/*.png 2>/dev/null | wc -l) -ge 10</automated>
  </verify>
  <acceptance_criteria>
    - baseline/screenshots/ contains screenshots covering each block-type (header, footer, hero, cards, form, nav/off-canvas, title-bar) across the relevant breakpoints among 1440/1024/768/390/360
    - DOM-measured values (not CSS source text) are recorded for each block in working notes for Task 2
    - No screenshots/findings exist for absent features (pricing/FAQ/modal)
  </acceptance_criteria>
  <done>Block-type screenshots and DOM measurements at the 5 viewports are captured as evidence for the visual-problem enumeration.</done>
</task>

<task type="auto">
  <name>Task 2: Write the AUD-02 visual-problems-by-block-type section</name>
  <files>.planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md</files>
  <read_first>
    - .planning/phases/01-baseline-audit-ui-design-contract/baseline/screenshots/ (evidence + the Task-1 notes)
    - .planning/phases/01-baseline-audit-ui-design-contract/baseline/axe/ (a11y violations to fold into findings)
    - .planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md (the contract findings will be reconciled against in 01-04)
  </read_first>
  <action>
    Fill the "## AUD-02 — Visual problems by block-type" section in 01-AUDIT.md (the placeholder heading from plan 01-01). Organize as one subsection per block-type: Header, Navigation (sticky + off-canvas), Hero/Slider, Service cards, Forms, Footer, Title-bar, Imagery, Global/typography. Under each, a table of findings with columns: # | What (the problem) | Where (block + which pages/representative page) | Viewport(s) (which of 1440/1024/768/390/360) | Why (the rendered/DOM evidence or axe rule ID) | Screenshot ref. Audit shared chrome ONCE and note page-specific deltas (RESEARCH Pattern 1) — do not repeat an identical header finding 11×.

    Fold relevant axe violations (from baseline/axe/) into the matching block-type as Why-evidence with the rule ID. Every finding's Why must cite a DOM measurement or an axe rule — not "looks off" (RESEARCH Pattern 2).

    Record the known mobile header search target-size flag as an "Accepted documented exception" (memory ui-audit-2026-06-23 + RESEARCH Open Question 2) unless this run surfaced it as a hard blocker — if it did, mark it as a real finding instead.

    Add an "## Absent features" note: pricing (CNV-02), FAQ accordion (CMP-05), and live modal (CMP-06) do not exist on any page — flag them as net-new-or-descope for later phases, NOT as audit findings of existing UI (RESEARCH Q3 + Anti-Pattern).
  </action>
  <verify>
    <automated>grep -qE 'AUD-02' .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md && grep -qiE 'absent' .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md && grep -qiE 'header|hero|footer' .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - The AUD-02 section is filled with per-block-type finding tables (what/where/viewport/why/screenshot)
    - Findings on shared chrome are stated once with page-specific deltas, not duplicated 11×
    - Every finding's Why cites a DOM measurement or an axe rule ID
    - The mobile header search target-size flag is recorded as an accepted exception (or a real finding if it blocked)
    - An "Absent features" note flags pricing/FAQ/modal as net-new-or-descope, not audited-as-present
    - No site code file was modified (git status shows only .planning/ changes)
  </acceptance_criteria>
  <done>01-AUDIT.md's AUD-02 section enumerates visual problems grouped by block-type, each backed by a DOM measurement or axe rule and a screenshot, with absent features explicitly flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local static server → localhost | python http.server :8080 serves the public static site to headless Chrome / Playwright on loopback |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information disclosure | Playwright screenshots committed under baseline/ | low | accept | Screenshots are of the public marketing site only; no secrets/PII rendered. Stored under .planning/ as audit evidence. |
| T-01-04 | Tampering | accidental site-code edit during measurement | low | mitigate | Plan measures only; verify `git status` shows only .planning/ changes. |

Documents-only phase; no installs, no network-exposed surface (RESEARCH Security Domain = N/A).
</threat_model>

<verification>
- baseline/screenshots/ has block-type screenshots across the 5 viewports
- 01-AUDIT.md AUD-02 section is filled with block-type finding tables citing DOM/axe evidence
- Absent features explicitly flagged; target-size flag recorded as exception
- `git status` shows changes only under .planning/ — no edits to css/*, js/*, or *.html
</verification>

<success_criteria>
A de-duplicated, evidence-backed visual-problem inventory by block-type exists for AUD-02, with zero site-code changes.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-audit-ui-design-contract/01-03-SUMMARY.md` when done.
</output>
