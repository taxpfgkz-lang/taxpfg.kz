---
phase: 03-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/custom.css
autonomous: true
requirements: [VIS-01, CMP-01, CMP-02, CMP-03, CMP-04]
must_haves:
  truths:
    - "Кнопки .pbmit-btn имеют все 4 состояния: hover/focus-visible/active/disabled; primary/secondary/ghost иерархия читаема и контрастна (AA)"
    - ".pbmit-btn:disabled выглядит выключенной (сниженная непрозрачность, cursor:not-allowed, без тени) и не реагирует на hover-подъём"
    - "Статус формы .pfg-form-status.is-error/.is-ok несёт НЕ только цвет (иконка/символ/рамка), различим при цветовой слепоте"
    - "Карточки .pfg-card в одном ряду визуально выровнены по высоте (C2 устранён) на 1024 и 768"
    - "Навигационные состояния (hover-underline-grow/active/focus-visible) консистентны; glass-эффект остаётся gated >=1201px; off-canvas меню не сломано"
    - "Единый визуальный язык (VIS-01): eyebrow/кнопки/карточки/фокус читаются одинаково на всех 11 страницах через токены Фазы 2"
  artifacts:
    - "css/custom.css — новая секция состояний компонентов (Этап 14): button :disabled+ghost, form-status non-color affordance, card height-equalize, nav scope-doc"
  key_links:
    - ".pbmit-btn override побеждает vendor по source-order (custom.css последний) при равной/+1 специфичности — net-new !important = 0"
    - "Декоративная карточная/кнопочная моторика уже в scoped reduced-motion блоке (custom.css:376-389) — новые декоративные переходы добавлять туда же"
---

<objective>
Закрыть пробелы состояний у уже-стилизованных компонентов (кнопки, формы, карточки, навигация) и свести их к единому визуальному языку (VIS-01) — всё через токены Фазы 2, без новых !important.

Это GAP-FILL, не greenfield: базовая стилизация .pbmit-btn / .pfg-form / .pfg-card / nav уже существует в custom.css. План добавляет недостающие состояния и устраняет один реальный дефект (C2 height-jitter).

Purpose: каждый интерактивный компонент должен декларировать hover/focus-visible/active/disabled и читаться системно на всех 11 страницах — фундамент для конверсии (Фаза 4) и a11y-прохода (Фаза 5).
Output: новая секция в css/custom.css с правилами состояний; net-new !important ≈ 0 (бюджет 59).
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-components/03-UI-SPEC.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-UI-SPEC.md

# Целевой файл (читать перед правкой — это append-only слой)
@css/custom.css
</context>

<tasks>

<!-- planner-discipline-allow: !important -->
<!-- planner-region-allow: !important -->

<task type="auto">
  <name>Task 1: Кнопки — :disabled состояние + явный ghost-tier (CMP-01)</name>
  <files>css/custom.css</files>
  <read_first>
    - css/custom.css:138-145 (контраст: текст #1b1b1b на золоте держит AA — primary)
    - css/custom.css:351-358 (референс focus-visible кольцо — уже покрывает .pbmit-btn)
    - css/custom.css:376-389 (scoped reduced-motion блок — декоративная моторика кнопок уже здесь)
    - css/custom.css:477-492 (раздел 9.5: present hover/active/blackish — пробелы видны)
    - .planning/phases/03-components/03-UI-SPEC.md (строка CMP-01: primary/secondary/ghost спека)
  </read_first>
  <action>
    Добавить в конец css/custom.css новую банер-секцию (русский комментарий, "Этап 14 — состояния компонентов", дата) и в ней правила, реализующие per D-CMP-01 недостающие состояния кнопок.

    1) :disabled — селектор .pbmit-btn:disabled, .pbmit-btn[disabled], .pbmit-btn.disabled: снизить opacity до ~.55, cursor:not-allowed, убрать box-shadow (none), и подавить hover-подъём (transform:none на :disabled:hover). Специфичность 0,2,0 / 0,1,1 — побеждает vendor по source-order, БЕЗ !important. На сайте сейчас нет disabled-кнопок в разметке — правило профилактическое и проверяется в изоляции (см. acceptance).

    2) Ghost-tier — определить явный класс .pbmit-btn.pfg-ghost: прозрачный фон, без тени, текст --pfg-gold-ink (держит AA на светлом, custom.css:96/396), на hover — фон transparent + подчёркивание/усиление --pfg-gold-ink (НЕ золотая заливка). Сохранить text-transform:none (русский). Ghost не вводится в разметку этим планом — это доступный tier для Фазы 4 CTA-иерархии; задача регистрирует стиль. Secondary-tier уже существует как .pbmit-btn.white / .blackish — задокументировать это русским комментарием, не дублировать.

    3) Декоративную моторику ghost/disabled (если добавляется transition) внести в существующий scoped reduced-motion блок custom.css:376-389, дописав селекторы — НЕ создавать новый media-блок и НЕ универсальный гаситель.

    Никаких изменений vendor-файлов, никакого @layer. Каждый !important (если окажется неизбежен) — с русским комментарием о перебиваемом vendor-правиле; цель net-new = 0.
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && test "$(grep -c '!important' css/custom.css)" -le 59 && echo IMPORTANT_OK</automated>
  </verify>
  <acceptance_criteria>
    - source: css/custom.css содержит селектор для .pbmit-btn:disabled с opacity < 1, cursor:not-allowed, box-shadow:none (grep подтверждает наличие всех трёх деклараций в правиле disabled)
    - source: css/custom.css содержит .pfg-ghost (или .pbmit-btn.pfg-ghost) правило с background transparent и цветом var(--pfg-gold-ink)
    - DOM @1440: на тест-странице (любая с .pbmit-btn) getComputedStyle активной кнопки показывает box-shadow != none (present-state не сломан)
    - budget: grep -c '!important' css/custom.css <= 59 (net-new = 0; любой новый цитирует vendor-правило русским комментарием)
    - комментарий секции на русском, объясняет ЗАЧЕМ (vendor-поведение/WCAG/контраст), per конвенция custom.css
  </acceptance_criteria>
  <done>Кнопки имеют явные :disabled и ghost-tier стили; present hover/focus/active не регрессировали; !important <= 59.</done>
</task>

<task type="auto">
  <name>Task 2: Форма — non-color affordance статуса + документирование nav-scope (CMP-02 visual, CMP-04)</name>
  <files>css/custom.css</files>
  <read_first>
    - css/custom.css:42-53 (.pfg-consent, .pfg-form-status.is-error/.is-ok — сейчас COLOR-ONLY)
    - css/custom.css:560-578 (раздел 9.9: поля формы + мягкое золотое фокус-кольцо — rest/focus уже есть)
    - css/custom.css:434-471 (раздел 9.3/9.4: glass-header gated >=1201px + nav underline-grow + off-canvas rationale)
    - js/custom.js:72-111 (initLeadForm — VER-04 DO-NOT-TOUCH; JS выставляет классы is-error/is-ok, поведение НЕ меняем)
    - .planning/phases/03-components/03-UI-SPEC.md (CMP-02 строка + "never color-only")
  </read_first>
  <action>
    В той же секции custom.css per D-CMP-02 и D-CMP-04:

    1) Non-color affordance для .pfg-form-status (CMP-02): к существующим color-only правилам .is-error (#d33) и .is-ok (#1a9e57) добавить НЕ-цветовой признак состояния. Использовать CSS-генерируемый префикс-символ через ::before (например предупреждающий глиф для is-error, галочка для is-ok) ИЛИ левую цветную рамку + усиление font-weight — что-то различимое без опоры на оттенок (WCAG 1.4.1). Глиф декоративный → если из иконочного шрифта, пометить aria-hidden не нужно (::before content не озвучивается). Текст статуса уже несёт сообщение (js/custom.js:84,105) — это семантический носитель; добавляемое — избыточный визуальный сигнал. role="status" aria-live="polite" в разметке уже есть (contacts.html:273) — НЕ трогать. НИКАКИХ правок js/custom.js: классы выставляет существующий initLeadForm, поведение байт-идентично (VER-04).

    2) Документировать nav-scope (CMP-04): добавить русский комментарий, фиксирующий, что glass-header намеренно gated >=1201px (off-canvas-safe, custom.css:426-433), underline-grow — декоративная моторика в reduced-motion блоке, а N1/N2 тап-таргеты навигации уже здоровы (>=44px по AUD-02) и не требуют правок. Это enforcement-документация, не новые правила; если какое-то состояние навигации (active/focus-visible) визуально не достаёт — добить через токены без !important. НЕ применять filter/transform/backdrop-filter к предкам off-canvas меню.
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && test "$(grep -c '!important' css/custom.css)" -le 59 && echo IMPORTANT_OK</automated>
  </verify>
  <acceptance_criteria>
    - source: css/custom.css правило .pfg-form-status.is-error содержит НЕ-цветовую декларацию (::before content, или border-left, или font-weight) дополнительно к color
    - source: css/custom.css правило .pfg-form-status.is-ok аналогично имеет non-color affordance
    - source: js/custom.js НЕ изменён в этом плане (git diff js/custom.js пуст) — VER-04
    - source: комментарий фиксирует nav glass-scope >=1201px и off-canvas-safe ограничение
    - budget: grep -c '!important' css/custom.css <= 59
  </acceptance_criteria>
  <done>Статус формы различим без цвета; nav-scope задокументирован; js/custom.js не тронут; !important <= 59.</done>
</task>

<task type="auto">
  <name>Task 3: Карточки — выравнивание высоты C2 + VIS-01 единый язык (CMP-03, VIS-01)</name>
  <files>css/custom.css</files>
  <read_first>
    - css/custom.css:72-74 (.pfg-card base: height:100%, .pfg-grid auto-fit minmax(260px,1fr))
    - css/custom.css:543-555 (раздел 9.8: hairline + тень + hover-подъём — rest/hover уже полны)
    - css/custom.css:376-389 (scoped reduced-motion — карточный lift уже здесь)
    - css/custom.css:391-420 (eyebrow ledger-stroke — сигнатура VIS-01)
    - .planning/phases/03-components/03-RESEARCH.md (Q3/A4: C2 — content/track-driven, не min-height-баг; DOM-подтвердить same-row)
  </read_first>
  <action>
    ДИАГНОСТИКА ПЕРЕД ФИКСОМ (требование чекера, как FT1): сначала DOM-измерить высоты соседних .pfg-card в ОДНОМ ряду на 1024 и 768 (Playwright getBoundingClientRect, http://127.0.0.1:8080, страница с .pfg-grid — services/accounting). Определить рычаг:
    - Если карточки в одном ряду УЖЕ равны (расхождение 283 vs 256 — это cross-breakpoint reflow, ожидаемо) → задокументировать как acceptable, добавить только подтверждающий комментарий + при необходимости align-items:stretch на .pfg-grid для гарантии.
    - Если соседние карточки в одном ряду НЕ равны → добавить выравнивание: убедиться что трек растягивает (.pfg-grid { align-items: stretch }) и .pfg-card { height:100% } уже заполняет; при контент-джиттере — токен --pfg-space-* min-height на согласованный заголовок/тело. Без жёстких px вне токенов.

    VIS-01 enforcement (per D-VIS-01): пройти все 11 страниц grep'ом на носители карточек/eyebrow/кнопок (.pfg-card, .pbmit-subtitle, .pbmit-btn) и убедиться, что визуальный язык один — palette frozen, eyebrow ledger-stroke, focus-кольцо, кнопочная иерархия читаются идентично через токены Фазы 2. Это проверка консистентности, НЕ новые токены и НЕ расширение палитры. Любое расхождение (per-page divergence) свести к общему правилу через токены без !important. Зафиксировать вывод DOM-диагностики C2 русским комментарием в секции.
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && test "$(grep -c '!important' css/custom.css)" -le 59 && echo IMPORTANT_OK</automated>
  </verify>
  <acceptance_criteria>
    - DOM @1024 и @768: соседние .pfg-card в одном grid-ряду имеют равную высоту (|h1-h2| <= 2px) ИЛИ зафиксирован русский комментарий-обоснование, что cross-breakpoint reflow приемлем (с числами измерения)
    - source: при добавлении выравнивания используются токены (--pfg-space-*), не сырые px вне сетки
    - VIS-01: grep по 11 HTML подтверждает единый носитель eyebrow (.pbmit-subtitle) и кнопок (.pbmit-btn) — нет per-page дивергентных классов карточек/CTA
    - budget: grep -c '!important' css/custom.css <= 59
    - DOM-диагностика C2 (числа) записана в комментарии перед фиксом — фикс не наложен вслепую
  </acceptance_criteria>
  <done>C2 height-jitter разрешён или обоснован числами; VIS-01 единство подтверждено; !important <= 59.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → browser | Единственная граница: статические CSS-правила рендерятся в браузере. Нет сервера, нет ввода, нет хранилища. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | css/custom.css (presentation-only) | low | accept | Правки только декларативный CSS; нет исполняемого кода, нет sink'ов. Vendor-файлы read-only. |
| T-03-02 | Tampering | initLeadForm (VER-04) | high | mitigate | js/custom.js НЕ изменяется в этом плане; статус формы — CSS-класс only. Поведение форма→WhatsApp байт-идентично. |

Примечание: пакеты не устанавливаются (no-build, нет package.json) → supply-chain checkpoint (T-SC) неприменим.
</threat_model>

<verification>
- grep -c '!important' css/custom.css <= 59 (net-new ≈ 0)
- git diff js/custom.js пуст (VER-04 — поведение не тронуто)
- DOM @1024/768: same-row .pfg-card высоты равны или обоснованы
- Полный phase-gate (DOM @5vp + axe + VER-04 smoke) выполняется в плане 03-03 (последняя волна), не здесь.
</verification>

<success_criteria>
Кнопки имеют :disabled + ghost; статус формы не color-only; карточки выровнены/обоснованы; VIS-01 единство подтверждено; js/custom.js не тронут; !important <= 59.
</success_criteria>

<output>
Create `.planning/phases/03-components/03-01-SUMMARY.md` when done
</output>
