---
phase: 03-components
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - services.html
  - contacts.html
  - css/custom.css
  - js/custom.js
autonomous: true
requirements: [CMP-05, CMP-06]
must_haves:
  truths:
    - "FAQ-аккордеон существует на services.html и contacts.html: native <details>/<summary> с классом .pfg-faq (НЕ .accordion — не задевает theme-handler scripts.js:309)"
    - "FAQ раскрывается/сворачивается мышью И клавиатурой (Enter/Space на <summary>); <summary> получает золотое focus-visible кольцо"
    - "FAQ-состояния (collapsed/expanded/focus) стилизованы токенами Фазы 2; expand-переход декоративный → в scoped reduced-motion блоке"
    - "Футер-ссылки ОБЕИХ колонок (Разделы + Услуги) имеют тап-зону >=44px на 390/360 (FT1 устранён по root-cause, не вслепую)"
    - "Лейбл согласия .pfg-consent имеет хит-зону >=44px (F2 устранён)"
    - "CMP-06 модалки честно descoped — Magnific не инстанцирован (0 триггеров на 11 страницах), модалка НЕ фабрикуется"
  artifacts:
    - "services.html — net-new FAQ <section> с .pfg-faq перед </main> (~line 450)"
    - "contacts.html — net-new FAQ <section> с .pfg-faq перед формой (~line 247)"
    - "css/custom.css — .pfg-faq стили (collapsed/expanded/focus) + FT1 footer hit-zone + F2 consent hit-zone"
    - "js/custom.js — initFaqA11y() ТОЛЬКО если native <details> требует ARIA-усилитель (честно оценить; иначе не добавлять)"
  key_links:
    - ".pfg-faq использует <details>, НЕ .accordion → theme-handler scripts.js:309 не матчит → нет интерференции"
    - "FT1 фикс через общий предок .site-footer .widget ul.menu li a — достаёт ОБЕ колонки (Разделы без .pbmit-two-column-menu обёртки)"
    - "FAQ-контент page-specific → НЕ shared chrome → change-all-11 НЕ триггерится; footer FT1 — CSS-only → тоже не триггерит markup-change-all-11"
---

<objective>
Построить единственный net-new компонент фазы — FAQ-аккордеон (CMP-05) на services.html и contacts.html — и закрыть два P1 тап-таргет дефекта (FT1 футер-ссылки, F2 лейбл согласия). Зафиксировать CMP-06 как честный descope.

Purpose: FAQ — конверсионный блок, снимающий возражения; должен работать с корректной семантикой/клавиатурой/ARIA из коробки. FT1/F2 поднимают тап-зоны до WCAG 2.5.5 floor (44px). CMP-06 не фабрикуется — Magnific не инстанцирован.
Output: FAQ-разметка на 2 страницах, .pfg-faq CSS, FT1/F2 CSS-фиксы, (опц.) initFaqA11y; net-new !important ≈ 0.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-components/03-UI-SPEC.md
@.planning/phases/03-components/03-RESEARCH.md

# Целевые файлы (читать перед правкой)
@css/custom.css
@js/custom.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: FAQ markup net-new на services.html и contacts.html (CMP-05)</name>
  <files>services.html, contacts.html</files>
  <read_first>
    - services.html:420-453 (конец main: CTA-секция .pfg-section--alt -> </main>; точка вставки FAQ перед </main> ~450)
    - contacts.html:247-276 (форма-секция; FAQ вставить ПЕРЕД ней ~247) и contacts.html:295-326 ("Остались вопросы?" eyebrow-паттерн как образец)
    - css/custom.css:391-420 (eyebrow .pbmit-subtitle/.pbmit-title — тот же heading-паттерн)
    - .planning/phases/03-components/03-RESEARCH.md (Code Examples CMP-05 native-разметка; Pitfall 3 — НЕ .accordion/.accordion-item)
  </read_first>
  <action>
    Вставить net-new FAQ-секцию per D-CMP-05, используя native <details>/<summary> (Claude's Discretion подтверждён research: бесплатные клавиатура/ARIA/[open], ноль связи с theme-handler).

    Каркас (повторяет существующий heading-паттерн темы):
    - section.pfg-section (или --alt для чередования) -> .container -> .pbmit-heading-subheading с p.pbmit-subtitle ("Частые вопросы") и h2.pbmit-title.
    - .pfg-faq контейнер -> несколько .pfg-faq-item = details.pfg-faq-item > summary (вопрос) + div.pfg-faq-panel > p (ответ).
    - КЛАСС .pfg-faq / .pfg-faq-item, НЕ .accordion / .accordion-item (Pitfall 3: scripts.js:309 биндится на .accordion глобально и сломал бы). Критично.

    Размещение:
    - services.html: перед </main> (~line 450, рядом с CTA-секцией). Вопросы про услуги.
    - contacts.html: перед секцией формы (section.pfg-section на line 247). Вопросы про обращение/онбординг/сроки.

    Контент (Claude's Discretion, 4-6 Q&A на страницу): корректный русский, предложный регистр, тематика бухгалтерско-налоговой фирмы (услуги/тарифы/онбординг/передача документов/сроки/форматы работы). Единственный net-new русский текст — master-контракт разрешает; остальные строки byte-identical (VER-04).

    FAQ НЕ shared chrome (контент разный) -> НЕ change-all-11; не добавлять на остальные 9 страниц.
    Декоративные глифы в разметке (если есть) — aria-hidden="true" инлайн. Внешних ссылок FAQ не добавляет; если добавятся — rel="noopener".
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && grep -l "pfg-faq" services.html contacts.html && (! grep -q 'class="accordion"' services.html contacts.html && echo NO_ACCORDION_OK)</automated>
  </verify>
  <acceptance_criteria>
    - source: services.html и contacts.html оба содержат class="pfg-faq" и хотя бы один <details class="pfg-faq-item">
    - source: НИ services.html НИ contacts.html не содержат class="accordion" в FAQ-разметке (grep -c == 0 для accordion) — иначе theme-handler перехватит
    - source: каждый FAQ имеет >=4 <details> элемента (разумный минимум Q&A)
    - source: остальные 9 HTML НЕ получили FAQ (grep pfg-faq по ним == 0) — FAQ не shared chrome
    - source: FAQ-копия — корректный русский, предложный регистр
  </acceptance_criteria>
  <done>FAQ-секции с native <details>.pfg-faq на services.html и contacts.html; контент 4-6 Q&A; .accordion не используется.</done>
</task>

<task type="auto">
  <name>Task 2: FAQ CSS-стили + опц. a11y-усилитель + FT1/F2 тап-зоны (CMP-05, тап-фиксы)</name>
  <files>css/custom.css, js/custom.js</files>
  <read_first>
    - css/custom.css:329-339 (токены --pfg-fs-body/--pfg-lh-* — размеры FAQ >=16px против iOS-зума, Pitfall 4)
    - css/custom.css:351-358 (focus-visible кольцо — <summary> не матчит a/button → добавить summary в группу)
    - css/custom.css:376-389 (scoped reduced-motion — expand-переход FAQ сюда)
    - css/custom.css:213-224 (footer .widget ul.menu контекст; :224 уже text-transform:none на ul.menu li a)
    - css/custom.css:627-640 (раздел 10.3: существующий 44px FT1-фикс — достаёт ТОЛЬКО .pbmit-two-column-menu, ≤575px)
    - css/custom.css:42-50, 642-655 (.pfg-consent layout + чекбокс 18px — F2 контекст)
    - js/custom.js:6-13,153-173 (initX-паттерн: IIFE, DOMContentLoaded, guard+early-return, idempotent — образец initSvgAria)
  </read_first>
  <action>
    Всё в той же секции custom.css (Этап 14) + опционально js/custom.js.

    1) FAQ CSS (CMP-05) per D-CMP-05:
    - .pfg-faq-item { border-bottom: 1px solid var(--pfg-hairline); }
    - .pfg-faq-item summary { cursor:pointer; font-size: var(--pfg-fs-body) (>=16px, против iOS-зума Pitfall 4); padding: var(--pfg-space-4) 0; list-style:none; } + summary::-webkit-details-marker{ display:none; } (свой chevron через ::after на token-цвете, поворот на [open]).
    - collapsed/expanded: панель .pfg-faq-panel отступы токенами; визуальный сдвиг chevron на details[open] > summary.
    - focus: ДОБАВИТЬ summary в существующую focus-visible группу (custom.css:351-358), чтобы <summary> получил золотое кольцо (research A3: summary не матчит текущую a/button-группу). hover — мягкий --pfg-gold-ink/подчёркивание.
    - text-transform:none если тема навяжет (русский).
    - Декоративный expand/rotate-переход внести в scoped reduced-motion блок (custom.css:376-389), дописав .pfg-faq-item summary::after — НЕ новый media, НЕ универсальный гаситель.

    2) JS a11y-усилитель (честная оценка): native <details>/<summary> уже даёт клавиатуру (Enter/Space), [open]-состояние и AT-семантику БЕЗ JS. initFaqA11y() добавлять ТОЛЬКО если нужен aria-усилитель сверх нативного (напр. синхронизация aria-expanded на summary для старых AT). Если не нужен — НЕ добавлять (research: native достаточно). Если добавляется — строго по initX-паттерну (guard early-return при отсутствии .pfg-faq, idempotent), зарегистрировать в DOMContentLoaded-списке (js/custom.js:6-13). НЕ трогать initLeadForm и прочее поведение (VER-04).

    3) FT1 footer hit-zone (root-cause фикс) per AUD-02:
    DOM-подтверждено в разметке: "Разделы" = col-md-2 > aside.widget > ul.menu (БЕЗ .pbmit-two-column-menu обёртки, contacts.html:370-373); "Услуги" = .pbmit-two-column-menu (contacts.html:381-384). Существующее правило custom.css:635 достаёт только "Услуги" и только ≤575px → "Разделы" остаются 26px. НЕ стекать ещё padding на старый селектор.
    Фикс: новое правило на ОБЩИЙ предок обеих групп — .site-footer .widget ul.menu li a { display:inline-block; padding-top:9px; padding-bottom:9px; } (line-height ~26 + 18 = ~44px). Специфичность 0,4,1 — побеждает vendor source-order, БЕЗ !important. CSS-only (разметка футера идентична на 11 страницах, но НЕ трогается) → change-all-11 НЕ триггерится. Перед финализацией селектора DOM-измерить ОБЕ колонки на 390/360.

    4) F2 consent hit-zone per AUD-02:
    .pfg-consent label сейчас ~36px. Поднять до >=44px через padding/min-height на .pfg-consent (или label-обёртке). CSS-only, токены/px по сетке, без !important.

    Каждый возможный !important — с русским комментарием о vendor-правиле; цель net-new = 0.
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && test "$(grep -c '!important' css/custom.css)" -le 59 && grep -q "site-footer .widget ul.menu li a" css/custom.css && grep -q "pfg-faq" css/custom.css && echo CSS_OK</automated>
  </verify>
  <acceptance_criteria>
    - source: css/custom.css содержит правило .pfg-faq-item summary с font-size var(--pfg-fs-body) (>=16px против iOS-зума) и list-style:none
    - source: <summary> добавлен в focus-visible группу (custom.css:351-358) — DOM @1440: Tab на summary даёт outline (золотое кольцо)
    - DOM @390 И @360: getBoundingClientRect().height >= 44 для ссылок ОБЕИХ футер-колонок ("Разделы" col-md-2 ul.menu li a И "Услуги" .pbmit-two-column-menu li a)
    - source: css/custom.css содержит селектор .site-footer .widget ul.menu li a (общий предок) — не стек на старый .pbmit-two-column-menu селектор
    - DOM @390/360: getBoundingClientRect().height >= 44 для .pfg-consent label (F2)
    - DOM: FAQ <details> переключает видимость .pfg-faq-panel по клику И по Enter/Space на <summary>
    - source: если initFaqA11y добавлен — он зарегистрирован в DOMContentLoaded-списке и имеет guard early-return; если не добавлен — git diff js/custom.js НЕ трогает initLeadForm (VER-04)
    - budget: grep -c '!important' css/custom.css <= 59
    - source: FAQ expand-переход (если декоративный) в scoped reduced-motion блоке, не универсальный
  </acceptance_criteria>
  <done>FAQ стилизован (collapsed/expanded/focus) токенами; <summary> с золотым кольцом; FT1 обе колонки >=44px; F2 >=44px; native клавиатура работает; !important <= 59.</done>
</task>

<task type="auto">
  <name>Task 3: CMP-06 модалки — зафиксировать честный descope</name>
  <files>css/custom.css</files>
  <read_first>
    - .planning/phases/03-components/03-UI-SPEC.md (CMP-06 descope-нота)
    - .planning/phases/03-components/03-RESEARCH.md (Q6: grep по 11 HTML = 0 триггеров)
  </read_first>
  <action>
    Подтвердить descope CMP-06 per D-CMP-06 (НЕ фабриковать модалку):
    1) Запустить grep по всем 11 HTML на триггеры Magnific (pbmin-lightbox-video, pbmit-lightbox, magnificPopup, mfp-) — ожидается 0 совпадений (контракт гейтит модальные правила "только если Magnific инстанцирован").
    2) Добавить в css/custom.css короткий русский комментарий-маркер в секции Этапа 14, фиксирующий: CMP-06 descoped — Magnific загружен, но 0 триггеров на 11 страницах (DOM/grep-подтверждено фаза 1 AUD-02 + research Q6); модальные стили/focus-trap НЕ создаются; если модалку инстанцируют позже — отдельная задача. Это документация descope, не код модалки.
    НЕ создавать никакой модальной разметки, CSS или JS.
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && test "$(grep -rc 'pbmin-lightbox-video\|pbmit-lightbox\|magnificPopup\|mfp-' *.html | grep -v ':0' | wc -l)" -eq 0 && echo MODAL_DESCOPE_OK</automated>
  </verify>
  <acceptance_criteria>
    - grep по 11 HTML на pbmin-lightbox-video|pbmit-lightbox|magnificPopup|mfp- == 0 совпадений (descope обоснован)
    - source: css/custom.css содержит русский комментарий, фиксирующий descope CMP-06 с причиной (Magnific не инстанцирован)
    - НЕ создано модальной разметки/CSS/JS (нет .pfg-modal или подобного)
  </acceptance_criteria>
  <done>CMP-06 честно descoped и задокументирован; модалка не фабрикуется; grep подтверждает 0 триггеров.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → browser | Статическая разметка + CSS рендерятся в браузере. FAQ-контент — статический авторский HTML, не пользовательский ввод. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | FAQ markup (services/contacts) | low | accept | FAQ — статический авторский HTML, нет injection-поверхности; не вводить innerHTML из недоверенного источника. |
| T-03-04 | Tampering | initFaqA11y (если добавлен) | medium | mitigate | Только idempotent ARIA-патч поверх native <details>; не трогает initLeadForm/WhatsApp-поток (VER-04). |
| T-03-05 | Tampering | новые внешние ссылки (если появятся в FAQ) | low | mitigate | FAQ внешних ссылок не добавляет; если добавятся — target=_blank rel=noopener (паттерн репо). |

Пакеты не устанавливаются (no-build) → supply-chain checkpoint неприменим.
</threat_model>

<verification>
- grep -c '!important' css/custom.css <= 59
- DOM @390/360: обе футер-колонки + consent label >= 44px height
- DOM: FAQ <details> toggles по клику и Enter/Space; <summary> получает focus-ring
- grep accordion в FAQ-разметке == 0 (theme-handler не перехватывает)
- grep Magnific-триггеров по 11 HTML == 0 (CMP-06 descope)
- Полный phase-gate (DOM @5vp + axe + VER-04 smoke) — план 03-03.
</verification>

<success_criteria>
FAQ работает (native клавиатура+ARIA) и стилизован на 2 страницах; FT1/F2 тап-зоны >=44px (root-cause фикс); CMP-06 descoped честно; js/custom.js поведение байт-идентично; !important <= 59.
</success_criteria>

<output>
Create `.planning/phases/03-components/03-02-SUMMARY.md` when done
</output>