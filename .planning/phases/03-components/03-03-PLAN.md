---
phase: 03-components
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified: []
autonomous: true
requirements: [VIS-01, CMP-01, CMP-02, CMP-03, CMP-04, CMP-05, CMP-06]
must_haves:
  truths:
    - "Все 11 страниц: DOM-measured @ 1440/1024/768/390/360 — нет горизонтального скролла, заголовки не клипаются, тап-таргеты >=44px (футер обе колонки, consent, FAQ summary)"
    - "axe (wcag2a/2aa/21a/21aa) = 0 нарушений на всех 11 страницах (AUD-01 floor)"
    - "Lighthouse accessibility >= 95 на каждой странице (AUD-01 floor; index/services/contacts = 96)"
    - "VER-04 smoke: форма→WhatsApp (номер 77072370050 + текст), мобильное меню, marquee, hero-слайдер, reduced-motion — байт/поведение-идентичны"
    - "FAQ не ломает theme-JS (scripts.js init не падает); <details> toggle + focus-ring работают"
    - "!important в custom.css <= 59 (net-new ≈ 0)"
  artifacts:
    - ".planning/phases/03-components/03-03-SUMMARY.md с числами DOM/axe/Lighthouse и VER-04 чеклистом"
  key_links:
    - "Верить DOM-измерению (getBoundingClientRect), а не CSS-тексту (Pitfall 2)"
    - "Workflow API proxy может падать — критичные проверки дублировать вручную через Playwright/axe CLI"
---

<objective>
Финальный phase-gate Фазы 3: доказать DOM-измерением на 5 брейкпоинтах + axe + Lighthouse + VER-04 smoke, что все компоненты стилизованы, FAQ работает с ARIA+клавиатурой, FT1/F2 тап-зоны >=44px, и ничего не регрессировало против AUD-01 floor.

Purpose: рекуррентный gate roadmap-фазы — без него фаза не закрывается. Проверяет результат планов 03-01 и 03-02 интегрально.
Output: 03-03-SUMMARY.md с числами и вердиктом; human-verify чекпоинт на визуальное подтверждение FAQ/состояний.
</objective>

<execution_context>
@C:/Users/Администратор/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Администратор/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-components/03-UI-SPEC.md
@.planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md

@css/custom.css
@js/custom.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOM-measured Playwright @ 5vp + axe + Lighthouse против AUD-01 floor</name>
  <files></files>
  <read_first>
    - .planning/phases/01-baseline-audit-ui-design-contract/01-AUDIT.md (AUD-01 floor: a11y>=95, axe=0; AUD-02 C2/F2/FT1 целевые)
    - css/custom.css (итог планов 03-01/03-02)
    - services.html, contacts.html (FAQ-разметка)
  </read_first>
  <action>
    Поднять локальный статический сервер (python -m http.server 8080 в корне репо) и прогнать измерительный набор против http://127.0.0.1:8080 на 11 страницах × 5 брейкпоинтов (1440/1024/768/390/360):

    1) Playwright DOM-measure (getBoundingClientRect, НЕ CSS-текст — Pitfall 2):
       - нет горизонтального скролла (scrollWidth <= clientWidth) на всех 11×5;
       - заголовки не клипаются;
       - тап-таргеты >=44px: футер-ссылки ОБЕИХ колонок (Разделы + Услуги) @390/360, .pfg-consent label @390/360, FAQ <summary> на services/contacts;
       - same-row .pfg-card высоты равны (или зафиксированное обоснование из 03-01);
       - FAQ <details> toggle по клику и Enter/Space; <summary> показывает focus-ring на Tab; кнопочные состояния present.
    2) axe (@axe-core/playwright, теги wcag2a/wcag2aa/wcag21a/wcag21aa) = 0 нарушений на всех 11.
    3) Lighthouse accessibility >= 95 на каждой (index/services/contacts >= 96). Windows/Cyrillic: EPERM rmSync при teardown — косметика, отчёты валидны (memory-нота).

    Workflow API proxy может падать (memory-нота) — критичные проверки (FT1 обе колонки, FAQ keyboard, axe) дублировать вручную через Playwright/axe CLI. Любую регрессию против floor — чинить в custom.css/js до прохождения (или вернуть на 03-01/03-02), не маскировать.

    Записать числа в 03-03-SUMMARY.md (per-page axe, min Lighthouse a11y, измеренные высоты тап-таргетов).
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && test "$(grep -c '!important' css/custom.css)" -le 59 && echo BUDGET_OK</automated>
  </verify>
  <acceptance_criteria>
    - DOM @1440/1024/768/390/360 × 11 страниц: scrollWidth <= clientWidth везде (нет h-скролла)
    - DOM @390/360: footer "Разделы" И "Услуги" ссылки height >= 44; .pfg-consent label height >= 44; FAQ summary height >= 44
    - axe: 0 нарушений (wcag2a/2aa/21a/21aa) на всех 11 страницах
    - Lighthouse a11y >= 95 на всех 11 (index/services/contacts >= 96)
    - DOM: FAQ toggle работает клик+Enter/Space; summary focus-ring виден
    - budget: grep -c '!important' css/custom.css <= 59
    - числа записаны в SUMMARY (не "выглядит ок" — конкретные значения)
  </acceptance_criteria>
  <done>Все 11×5 DOM-проверок зелёные; axe=0; Lighthouse>=95; тап-таргеты >=44px; числа в SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: VER-04 JS-smoke — бизнес-логика байт/поведение-идентична</name>
  <files></files>
  <read_first>
    - js/custom.js:6-13 (DOMContentLoaded init-список — initFaqA11y зарегистрирован?)
    - js/custom.js:72-111 (initLeadForm — VER-04 do-not-touch; номер 77072370050, encodeURIComponent)
    - js/custom.js:131-146 (initMarqueeSpeed), :180+ (initSwiperA11y)
  </read_first>
  <action>
    Прогнать VER-04 smoke на живых страницах (http://127.0.0.1:8080), подтвердить поведение-идентичность:
    1) Форма→WhatsApp (contacts.html): заполнить, submit → window.open на https://wa.me/77072370050?text=... с корректным encodeURIComponent-сообщением; consent-валидация даёт is-error при пустом согласии. Номер и текст не изменились.
    2) Мобильное меню: открыть/закрыть на ≤1200px (off-canvas не схлопнут — высота 100vh, не 49px).
    3) Marquee: бегущая строка услуг идёт штатной скоростью (Swiper inline transition не сломан).
    4) Hero-слайдер: появление заголовка/кнопки работает.
    5) reduced-motion: при включённом флаге декоративная моторика (карточки/кнопки/nav-underline/FAQ-expand) гаснет, а brand-motion (Swiper/marquee/GSAP/AOS) СОХРАНЯЕТСЯ.
    6) FAQ не ломает theme-JS: scripts.js init на services/contacts не бросает ошибок в консоли; .accordion-handler не перехватывает .pfg-faq.
    git diff js/custom.js: изменения — ТОЛЬКО регистрация/тело initFaqA11y (если добавлен) или пусто; initLeadForm/WA_NUMBER/текст не тронуты.
    Записать чеклист в SUMMARY.
  </action>
  <verify>
    <automated>cd "C:/Users/Администратор/Desktop/taxpfg.kz" && grep -q "77072370050" js/custom.js && echo WA_NUMBER_INTACT</automated>
  </verify>
  <acceptance_criteria>
    - DOM/runtime: submit формы открывает https://wa.me/77072370050 с encodeURIComponent-текстом; пустой consent → .pfg-form-status.is-error
    - source: js/custom.js содержит "77072370050" (номер не изменён); git diff не трогает initLeadForm-тело
    - runtime: мобильное меню off-canvas открывается на высоту 100vh (не 49px — off-canvas-safe не нарушен)
    - runtime: marquee и hero-слайдер работают штатно (brand-motion floor)
    - runtime: при prefers-reduced-motion декоративная моторика гаснет, brand-motion сохраняется
    - runtime: консоль без новых ошибок на services/contacts; .pfg-faq не перехвачен theme accordion-handler
    - VER-04 чеклист записан в SUMMARY
  </acceptance_criteria>
  <done>VER-04 поведение байт/поведение-идентично; off-canvas не сломан; FAQ не ломает theme-JS; чеклист в SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Визуальное подтверждение состояний и FAQ (human-check)</name>
  <files></files>
  <action>
    Завершающее визуальное подтверждение Фазы 3 человеком после прохождения всех автоматических проверок (DOM @5vp, axe=0, Lighthouse>=95, VER-04 smoke, !important<=59), записанных в 03-03-SUMMARY.md. Это не код — это финальный human-check визуала компонентов и FAQ.
  </action>
  <verify>
    <human-check>
      1. Запусти статический сервер: в корне репо `python -m http.server 8080`.
      2. Открой http://127.0.0.1:8080/services.html и http://127.0.0.1:8080/contacts.html — найди FAQ-секцию ("Частые вопросы"). Кликни вопрос: ответ раскрывается/сворачивается. Tab до вопроса — видно золотое фокус-кольцо; Enter/Space раскрывает.
      3. На contacts.html отправь форму без галочки согласия — статус показывает ошибку НЕ только цветом (символ/рамка). Поставь галочку, заполни, отправь — открывается WhatsApp на +7 707 237 00 50.
      4. Сузь окно до ≤390px: футер-ссылки обеих колонок («Разделы» и «Услуги») и чекбокс согласия — комфортный тап (>=44px), без горизонтального скролла.
      5. Проверь единообразие кнопок/карточек/eyebrow на index/about/services/contacts (VIS-01).
    </human-check>
  </verify>
  <done>Человек подтвердил: FAQ работает (клик+клавиатура+фокус), статус формы различим без цвета, форма→WhatsApp открывается на верный номер, тап-таргеты комфортны на мобильном, визуальный язык единый.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → browser | Только верификация (чтение/измерение рендера). Кода не добавляется. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Spoofing | VER-04 smoke (форма→WhatsApp) | high | mitigate | Smoke явно проверяет неизменность номера 77072370050, текста и encodeURIComponent-потока — регрессия поведения ловится здесь. |
| T-03-07 | Tampering | результаты verify (proxy-сбой) | medium | mitigate | Workflow API proxy может падать (memory-нота) → критичные проверки дублируются вручную через Playwright/axe CLI; верить DOM, не CSS-тексту. |

Пакеты не устанавливаются → supply-chain checkpoint неприменим.
</threat_model>

<verification>
- DOM @1440/1024/768/390/360 × 11: нет h-скролла, тап-таргеты >=44px, заголовки не клипаются
- axe = 0 на 11 страницах; Lighthouse a11y >= 95 (index/services/contacts >= 96)
- VER-04 smoke: форма→WhatsApp/меню/marquee/слайдер/reduced-motion байт/поведение-идентичны
- grep -c '!important' css/custom.css <= 59
- Все числа в 03-03-SUMMARY.md; human-verify approved
</verification>

<success_criteria>
Все автоматические проверки зелёные против AUD-01 floor; VER-04 поведение-идентично; FAQ работает; тап-таргеты >=44px; !important<=59; человек подтвердил визуально.
</success_criteria>

<output>
Create `.planning/phases/03-components/03-03-SUMMARY.md` when done
</output>