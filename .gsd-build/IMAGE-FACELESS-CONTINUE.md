# ⏸ CONTINUE — замена изображений на «безликие» (taxpfg.kz)

> Передача состояния подзадачи поверх готового сайта (пауза `/gsd-pause-work`).
> Обновлён: 2026-06-20 (+05). Родительский контекст проекта — `.continue-here.md` (треки A/B, сборка).
> Возобновление: прочитай этот файл → действуй с раздела «8. Как возобновить».

---

## 0. Суть задачи (9 этапов)

Заменить ВСЕ изображения сайта, содержащие узнаваемые человеческие лица, на **безликие**
(без распознаваемых лиц), сохранив визуальную когерентность, чистоту лицензий, перформанс.
Приоритеты: 1) когерентность 2) безликость 3) лицензии 4) перформанс 5) код 6) доступность 7) минимальность.
**Жёсткое правило честности:** не утверждать «заменил / скачал / проверил в браузере», если это фактически не сделано.

## 1. Аудит: 7 изображений с лицами (бэкап оригиналов на диске)

`processbox-img-01` и `processbox-img-04` проверены — БЕЗ лиц, оставлены как есть.
Бэкап всех 7 оригиналов: `.gsd-build/img-backup/faces-2026-06-20/`.

| # | файл | размеры | формат | статус |
|---|------|---------|--------|--------|
| 1 | images/infobox-img.png      | 120×40  | RGBA | ✅ ЗАМЕНЁН — сгенерирован безликий бейдж (4 кружка + «+») |
| 2 | images/service-left-img.png | 482×740 | RGBA | ✅ ЗАМЕНЁН — флэт-дашборд, 547KB→48KB |
| 3 | images/about-01.jpg         | 900×1000| JPEG RGB | ⏳ кандидат ВЫБРАН, НЕ размещён |
| 4 | images/about-02.png         | 381×680 | PNG RGB | ⏳ кандидат ВЫБРАН, НЕ размещён |
| 5 | images/static-box-02.jpg    | 600×420 | JPEG RGB | ⏳ кандидаты НЕ просмотрены |
| 6 | images/processbox-img-02.png| 505×389 | PNG RGB | ⏳ кандидаты НЕ просмотрены |
| 7 | images/processbox-img-03.png| 505×389 | PNG RGB | ⏳ кандидаты НЕ просмотрены |

> ⚠️ **ГЛАВНОЕ:** п.3–7 (5 фото-слотов) на сайте **ВСЁ ЕЩЁ показывают лица** — оригиналы в `images/` не тронуты.
> Задача НЕ завершена. Фактически заменены только 2 графических ассета (#1, #2).

## 2. Стратегия (зафиксирована)

- **Drop-in замена** (то же имя/размеры/формат) → НИ ОДНОЙ правки HTML/фрагментов для самой замены.
  Правим только `alt` там, где картинка смысловая.
- Фото: `python .gsd-build/place_photo.py <src> <dest> <W> <H> [focus_y=0.5] [focus_x=0.5]`
  — cover-crop LANCZOS до точных размеров, сохранение в формат назначения (JPEG q86 progressive / PNG optimize).
- Графика: `.gsd-build/gen_faceless.py` (бренд-палитра; уже отработал для #1, #2).
- Палитра (css/base.css): gold `#ecab23`, amber `#f59f46`, navy `#16222d`, текст `#525d62`, светлый `#ecf0f4`.
- **Безликость проверяется СВОИМИ ГЛАЗАМИ** (Read каждого кандидата) до любого заявления о размещении.
- Запуск python на этом Windows-хосте: префикс `PYTHONIOENCODING=utf-8 PYTHONUTF8=1`.

## 3. Пулы кандидатов — `.gsd-build/img-src/<slot>/`

Превью-монтажи: `.gsd-build/img-src/_preview/`; аудит-монтажи лиц: `.gsd-build/_audit_*.png`.

- about-01: `cand-1` 1400×933, `cand-2` 1400×933, `cand-3` 1400×1867
- about-02: `cand-1` 1400×2100, `cand-2`, `cand-3`, `new-a`, `new-b`, `new-c`
- static-box-02: `cand-1`, `cand-2`, `cand-3` (~1400×933)
- processbox-img-02: `cand-1`, `cand-2`, `cand-3` (~1400×934)
- processbox-img-03: `cand-1`, `cand-2`, `cand-3` (~1400×934)

## 4. Финальный отбор своими глазами — где стоим

- ✅ **about-01 → cand-1**: светлый белый стол — ноутбук + растение + документ «CONTRACT №» + ручка + блокнот.
  Безликое, тёплое, премиальное, по теме. 1400×933 → cover-crop в портрет 900×1000.
  Команда: `python .gsd-build/place_photo.py .gsd-build/img-src/about-01/cand-1.jpg images/about-01.jpg 900 1000 0.5 0.45`
- ✅ **about-02 → cand-1**: «INCOME TAX» (от руки) + чёрный калькулятор + красная ручка на белом.
  Безликое, прямо по теме «налоги», светлое, нативный портрет 1400×2100.
  Команда: `python .gsd-build/place_photo.py .gsd-build/img-src/about-02/cand-1.jpg images/about-02.png 381 680`
  Резерв: `cand-3` («Financial reports» + телефон-калькулятор). Отклонены: `new-a`/`new-b` (тема трейдинг/ритейл, темнее).
  ⚠️ `new-c` — API не отдал превью на просмотр; **вслепую не брать**.
- ⏳ **static-box-02** → НЕ просмотрено (`cand-1/2/3`). Слот 600×420 landscape, фон карточки услуги (index.html:697 фон + :699 img).
- ⏳ **processbox-img-02** → НЕ просмотрено (`cand-1/2/3`). Слот 505×389 (index.html:812).
- ⏳ **processbox-img-03** → НЕ просмотрено. ⚠️ `cand-1` ранее помечен: возможна сильно размытая фигура человека на фоне —
  ПРОВЕРИТЬ своими глазами до использования; при сомнении брать `cand-2`/`cand-3`. Слот 505×389 (index.html:815).

## 5. Карта ссылок (grep-подтверждено) — для alt-текстов и проверки 404

index.html:
- `:337` about-01.jpg  `alt=""`  → дать осмысленный RU-alt (блок «О компании», картинка содержательная)
- `:341` about-02.png  `alt=""`  → осмысленный RU-alt либо `alt=""` если решим, что декоративная
- `:350` infobox-img.png  `alt="Бухгалтерия и налоги под ключ"` (ассет уже заменён; alt оставить/уточнить)
- `:404` service-left-img.png  `alt=""` (заменён; декоративная иллюстрация — `alt=""` ок)
- `:697` static-box-02.jpg (CSS-фон) + `:699` static-box-02.jpg `<img alt="">`
- `:812` processbox-img-02.png  `alt=""`
- `:815` processbox-img-03.png  `alt=""`
services.html:200 + `.gsd-build/frag/services.content.html:20` — service-left-img.png `alt=""`.
**Имена файлов НЕ меняются → фрагмент services править НЕ нужно.**

## 6. Что осталось (точный порядок)

1. Досмотреть кандидатов 3 слотов (static-box-02, processbox-img-02, processbox-img-03), выбрать безликие.
   ⚠️ processbox-img-03/`cand-1` — проверить на размытую фигуру.
2. Разместить 5 фото drop-in через `place_photo.py` (about-01/about-02 — командами из §4; остальные — по размерам §1).
3. Alt-тексты: index.html `:337`, `:341` (+ при необходимости `:350`) — RU, функциональные, без SEO-спама.
4. Обновить `images/CREDITS.txt`: добавить about-01/about-02/static-box-02/processbox-02/03 + примечание о генерации
   infobox/service-left. Лицензия — как у текущего набора (Unsplash/Pexels License, атрибуция не требуется).
   ⚠️ ЧЕСТНО: per-candidate URL/автор при загрузке НЕ логировались (как и в текущем CREDITS) — указать это прямо.
5. Проверка (этап 8): `PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python -m http.server 8127 --bind 127.0.0.1`;
   404/битые ссылки; своими глазами — лиц нет, вёрстка цела, текст-оверлей читаем, адаптив (1440/768/390);
   Lighthouse не хуже; скриншоты chrome-devtools/Playwright MCP (если доступно — иначе честно отметить, что не проверено).
6. Финальный отчёт на русском (этап 9): счётчики, таблица замен, изменённые файлы, проверки, риски, вердикт production-ready.

## 7. Состояние git (на момент паузы)

- Заменены (вошли в WIP-коммит паузы): `images/infobox-img.png`, `images/service-left-img.png`.
- Новое (в WIP): `.gsd-build/{gen_faceless.py, place_photo.py, img-src/, _audit_*.png, _*.jpg/png scratch}` + этот файл.
- Бэкап оригиналов: `.gsd-build/img-backup/faces-2026-06-20/` (на диске).
- **Откат любого слота:** скопировать оригинал из бэкапа обратно в `images/`.

## 8. Как возобновить

```
1. Прочитать этот файл + родительский .continue-here.md.
2. cd C:/Users/Администратор/Desktop/taxpfg.kz
3. Досмотреть 3 слота (Read кандидатов), выбрать безликие (§4, §6.1).
4. Разместить 5 фото (place_photo.py, §4/§6.2) — НЕ заявлять о замене без фактического запуска команды.
5. Alt + CREDITS (§6.3–4). Проверка (§6.5). Отчёт (§6.6). Атомарные коммиты по ходу.
```

**Следующее конкретное действие:** `Read` кандидатов `static-box-02` (cand-1/2/3) → `processbox-img-02` →
`processbox-img-03` (cand-1 — проверить на размытую фигуру); выбрать по одному безликому; разместить все 5
через `place_photo.py`; затем alt-тексты, CREDITS, проверка, отчёт.
